# 저장소 안에서는 항상 LF 로 보관한다. Prettier 의 endOfLine: "lf" 설정과 맞춘다.
# (Windows/macOS 를 섞어 쓸 때 줄바꿈만 바뀐 diff 가 생기는 걸 막는다.)
* text=auto eol=lf

# 바이너리로 취급 — 줄바꿈 변환 대상에서 제외
*.png binary
*.jpg binary
*.otf binary
*.ttf binary
