; Expo SDK 57은 react-dom(웹 전용) transitive peer가 react 버전과 어긋나 있어
; 순정 npm install이 ERESOLVE로 실패합니다. `npx expo install`이 내부적으로 쓰는
; 것과 동일한 정책을 팀 전체에 고정해, 누가 어떤 명령으로 설치하든 동일하게 풀리도록 합니다.
legacy-peer-deps=true
