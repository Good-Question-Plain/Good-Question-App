# 인수인계 (2026-08-17 마감)

작업 규칙은 [AGENTS.md](AGENTS.md), 실행 방법은 [README.md](README.md) 에 있다.
이 문서는 **둘 중 어디에도 없는 것** — 지금까지의 판단과 다음에 할 일 — 만 담는다.

> ## 다음 세션은 여기부터
>
> **08-17 에 전 흐름을 실기기로 끝까지 돌렸다.** 로그인 → 이야기 → 대화 9장면 →
> 순서 맞추기 → 다시 말하기(STT 실동작) → 완료 → 홈 → 단어장까지 이어진다.
> `/main` 404 와 사진 객체 키는 서버에서 해결됐다.
>
> **그런데 훨씬 큰 게 나왔다 — [대화가 통째로 열리지 않는다](#1-1-️-대화가-통째로-열리지-않는다--모든-장면이-kind-narration).**
> 서버가 9장면을 **전부 `kind: narration`** 으로 준다. 마이크가 한 번도 안 열리고
> `speak` 를 못 탄다. 씬 단어·미션도 데이터가 비어 있다. **앱은 고칠 게 없고
> 백엔드가 고쳐야 한다.** 이게 풀리기 전엔 대화·단어장·리포트가 다 빈 채로 돈다.
>
> 1. **[지금 막혀 있는 것](#1-지금-막혀-있는-것-백엔드)** — 전부 백엔드다.
>    `kind` · 호격 조사 · 핵심 단어 빈 응답 · 세션 리셋 · **이미지가 키/URL 섞여 옴**
>
> ⚠️ **시연 기기에 한국어 TTS 음성이 없다**(3-5). 아이가 글을 못 읽는 게 전제인
> 앱이라 읽어주기가 안 되면 제품이 성립하지 않는다 — 기기 점검 목록에 넣을 것.
> 2. **[다음 세션이 할 일](#2-다음-세션이-할-일--테스트-위주)** — 리포트 화면이
>    유일하게 못 본 화면이다(보호자 비밀번호가 필요하다) · 오류/빈 상태 흔들기
> 3. **[08-17 에 고친 것](#3-08-17-에-고친-것)** · **[08-16 에 고친 것](#4-08-16-에-고친-것--회귀-방지)** —
>    회귀 방지용. 원인까지 적어뒀다
>
> 아래 "디자인 구현" 절(2026-08-09 ~ 08-12)은 끝난 이야기다. 다만 **왜 그렇게
> 만들었는지**가 거기 있어서, 화면을 고칠 일이 생기면 해당 절을 본다.
## 어디까지 됐나

Figma 섹션 `182:303`(퍼블리싱 해도 되는 페이지)에서 **그때까지 올라와 있던 화면은 전부 구현**했다.

로그인 / 회원가입(4단계) / 비밀번호 찾기(3단계) / 아이 프로필 생성·선택 /
메인 화면(홈) / 이야기 목록·상세 / 이야기 전개 및 대화(도입·대화·미션) /
이야기 후 활동(순서 맞추기·다시 말하기·완료) /
단어장(전체·이야기별) / 단어 상세 / 마이페이지 / 보호자 리포트 /
아이 전환 드롭다운 / 모달 3개(보호자 확인·로그아웃·회원탈퇴)

전부 안드로이드 태블릿 에뮬레이터(1280×800 dp)에서 눌러가며 확인했고, 주요 화면은
브라우저에서 computed style 을 뽑아 Figma 실측값과 수치 대조까지 했다.

## 뒤늦게 올라온 디자인 — 전부 구현했다

아래는 2026-08-09 에 올라온 것들이고, 마지막 줄만 2026-08-11 에 퍼블리싱 쪽으로
옮겨져 그때 구현했다.

| 화면                 | node                                    | 어디에 있나                                   |
| -------------------- | --------------------------------------- | --------------------------------------------- |
| 메인 화면(홈)        | `78:184`                                | `story/screens/HomeScreen`                    |
| 이야기 도입          | `86:410`                                | `StoryPlayScreen` 의 `blocked` 상태           |
| 이야기 전개 및 대화  | `161:1158`                              | `StoryPlayScreen` 의 `listening` 상태         |
| 대화(미션 있을 시)   | `216:277`                               | `StoryPlayScreen` 의 미션 패널                |
| 마이크 상태 가이드   | `86:448`                                | 화면이 아니라 스펙. `MicControl` 4상태의 근거 |
| 활동 1/2 순서 맞추기 | `92:993` `92:1094` `92:1157` `92:1215`  | `StoryActivityScreen` (초기·진행·오답·정답)   |
| 활동 2/2 다시 말하기 | `92:1290`                               | `StoryRetellScreen`                           |
| 활동 완료            | `92:1340`                               | `StoryDoneScreen`                             |
| 보호자 리포트        | `252:193` `252:249` `252:297` `287:273` | `report/screens/ReportScreen` (2026-08-11)    |

이야기 하나가 이렇게 이어진다.

```
목록 → 상세 → /story/[id]/play → /activity → /retell → /done → 홈 또는 목록
```

리포트는 이 흐름과 따로 떨어져 있다 — 마이페이지에서 보호자 확인을 통과해 들어간다.

```
/mypage → 학습 리포트 보기 → (보호자 확인) → /report
```

### 파일에 섹션이 두 개다 (2026-08-11 갱신)

캔버스 최상위에 섹션이 둘 있다.

| 섹션      | 이름                             | 상태                                   |
| --------- | -------------------------------- | -------------------------------------- |
| `182:303` | 퍼블리싱 해도 되는 페이지        | **전부 구현 완료** (최대 노드 287:296) |
| `182:304` | 아직 퍼블리싱 하면 안되는 페이지 | 옛 버전 `118:210` 하나만 남았다        |

**2026-08-09 에 "구현하지 말라"고 적어둔 학습 리포트가 그 뒤 퍼블리싱 쪽으로
옮겨져서, 이번에 구현했다.** `252:193` `252:249` `252:297` 이 `182:303` 안의
새 섹션 `287:262`(보호자 리포트)로 이동했다. `182:304` 에는 예전 주간/월간 요약
시안(`118:210`)만 남아 있고, 이건 지금 화면과 구성이 달라 손대지 않았다.

이때 같이 생긴 것:

| 노드      | 이름        | 처리                                          |
| --------- | ----------- | --------------------------------------------- |
| `287:273` | 비활성화 시 | 이전/다음 리포트 버튼의 비활성 상태. 반영했다 |
| `287:296` | 메인페이지  | 기존 `78:184` 을 감싼 섹션일 뿐. 새 내용 없다 |

**다음에 확인할 것**: 두 섹션에 새 프레임이 생겼는지, 그리고 `182:304` 의
`118:210` 이 다듬어져 넘어오는지를 본다. 노드 ID 가 클수록 최근에 만든 것이다.

### 아이 전환은 모달이 아니라 드롭다운이다 (2026-08-12)

같은 동작에 시안이 두 개 있다. 섹션 전체를 훑다가 어디에도 매핑되지 않는 노드로
발견했다.

| 노드                | 무엇                                | 상태                         |
| ------------------- | ----------------------------------- | ---------------------------- |
| `181:889` `181:920` | 알약 아래 펼쳐지는 드롭다운         | **이걸 구현했다**            |
| `86:734`            | 화면 가운데 "아이 프로필 선택" 모달 | 구현돼 있지만 지금은 안 쓴다 |

드롭다운이 노드 번호가 더 나중이고, 보호자 리포트·메인페이지 섹션에 각각 하나씩
알약 자리에 놓여 있어 현재 의도로 봤다(사용자 확인을 받았다).

`ChildSwitcher` 가 목록을 직접 들고 펼친다 — 알약의 화면 좌표를 `measureInWindow`
로 재서 그 자리에 겹쳐 그리는 방식이다. 첫 줄이 알약을 덮고 화살표만 뒤집힌다.
줄 높이는 디자인 43 대신 48로 올렸다(줄이 붙어 있어 hitSlop 으로 못 넓힌다).

**`ChildProfileModal` 은 남겨뒀지만 지금 아무 데서도 쓰지 않는다.** `86:734` 시안이
살아 있어서 지운 게 아니다. 모달로 되돌리려면 라우트에서 다시 붙이면 된다.

**드롭다운에는 "아이 추가하기"가 없다** — 디자인에 없다. 추가는 마이페이지에서 한다.

### 리포트에도 이야기 선택 드롭다운이 생겼다 (2026-08-12)

이야기 줄(`252:202`)에 화살표가 붙어(`320:194`) 트리거가 됐고, 펼치면 `320:197`
(244×168, 56짜리 줄)이 나온다. 다른 이야기의 리포트로 바로 넘어간다
(`features/report/components/StoryPicker`). "이전/다음 리포트 보기" 버튼은 그대로 둔다.

**목록 순서는 저장 순서(오래된 것 → 최신) 그대로 두고 현재 것에 화살표를 달았다.**
시안은 첫 줄에만 화살표가 있는데 줄 내용이 전부 자리표시라 "첫 줄 = 현재"인지
"목록 전체 + 현재 표시"인지 구분되지 않는다. 아래 이전/다음 버튼과 순서를 맞추는
쪽을 골랐다. **디자이너 확인이 되면 바꾸면 된다.**

### 디자인이 바뀌었는지 확인하는 법

노드를 눈으로 훑지 말고 스냅샷을 비교한다. `get_metadata(0:1)` 응답이 파일로
떨어지므로, 이전 응답 파일과 지금 것을 파싱해 노드 id·이름·크기를 비교하면
추가/삭제/크기변경이 그대로 나온다. 2026-08-12 에 이 방법으로
**추가 91 · 삭제 72 · 크기변경 11** 을 찾았다 (전부 대화 화면과 리포트 드롭다운).

여기까지 훑은 방법:

```bash
# 최상위 구조만 (섹션 목록)
get_metadata(fileKey) → 페이지 목록 → get_metadata(nodeId: "0:1")
# 응답이 크면 파일로 떨어지니 들여쓰기로 잘라 본다
```

### 마이페이지 노드가 바뀌었다

`118:291` → `234:567`. 내용은 같아 재작업은 없었고, 코드 주석의 노드 번호만 갱신했다.

## 홈에서 내린 판단들

**아이 전환은 라우트에서 조립한다.** `HomeScreen` 은 story feature 에 있고
`childName` / `childOptions` / `onSelectChild` 만 받는다. 아이 목록은
`src/app/(tabs)/home.tsx` 가 넘긴다 — story 가 child 를 직접 가져오면 feature 끼리
의존이 생긴다. (활성 아이는 그 뒤 전역 스토어로 올라갔다 —
`features/child/model/activeChildStore.ts`.)

**"이어하기" 버튼 색을 디자인과 다르게 썼다.** 디자인은 `#FFA833`(primary/400)에
Arial 라벨인데, 이 프레임엔 Figma Variable 이 하나도 안 걸려 있고 다른 화면의 주
CTA 는 전부 `#FF9200`(primary/500)이다. 미완성 시안으로 보고 토큰(`primary`)으로
맞췄다. 크기(h49 / 20 Bold)는 디자인대로 `Button size="xl"` 을 새로 만들었다.

**전환 알약 배경색을 뒤집었다.** 디자인은 흰 배경 위 `#F8F9FA` 알약인데, 홈의
페이지 배경이 그 `#F8F9FA` 라 그대로 두면 알약이 안 보인다. 흰 알약으로 바꿔
대비 관계만 유지했다. **바탕이 흰 화면(리포트)에서는 반대로 디자인 원본을 써야
한다** — `ChildSwitcher` 의 `tone` prop 이 그것이다.

**썸네일 그림은 실측(315)대로 두고 잘리게 놔뒀다.** 그림이 썸네일 높이(297)를
넘어서 이야기에 따라 위아래가 잘리는데, 크게 보이는 쪽이 낫다는 판단을 받았다.

**추천 카드는 폭에 따라 썸네일이 줄어든다.** 디자인의 191px 을 분할 화면(768dp)에서
그대로 쓰면 "약 12분"이 세로로 쪼개진다. `useResponsive().select` 로 104/140/191.

**인사말 조사를 계산한다.** 받침이 있으면 "규한아", 없으면 "지오야"
(`HomeScreen` 의 `vocativeParticle`). 디자인엔 "지오야" 하나뿐이라 규칙은 직접 넣었다.

## 대화 화면에서 내린 판단들

### 2026-08-12 에 2단으로 전면 재설계됐다

시안이 바뀌어 다시 만들었다. 이전 노드(`86:410` 도입, `216:277` 미션)는 **삭제**되고
`380:342` / `380:281` 로 교체됐다. `161:1158`(대화)은 ID 만 남고 내용이 전부 바뀌었다.

| 무엇        | 이전                       | 지금                             |
| ----------- | -------------------------- | -------------------------------- |
| 레이아웃    | 배경 전체 + 가로 패널(944) | **2단** (왼쪽 457 : 오른쪽 539)  |
| 줄거리      | 배경 위 좁은 판            | 왼쪽 열을 세로로 꽉 채우는 큰 판 |
| 배경 그림   | 화면 전체                  | 오른쪽 열만 (539×687)            |
| 대화·마이크 | 화면 가운데                | 오른쪽 열 안                     |

**왼쪽 패널은 새 컴포넌트다** (`NarrationPanel`, 380:274). `#FFFBF6` 바탕에 radius 16,
왼쪽 위 뱃지(15/Bold), 오른쪽 아래 "다시 듣기". 미션 패널은 여전히 옛 모양이라
`ScenePanel` 을 그대로 쓴다 — 뱃지 크기도 미션만 예전 것(12)이다.

**비율은 flex 실측값으로 넣었다** (`flex: 457` / `flex: 539`, 사이 13). 오른쪽 열은
화면 오른쪽·아래 끝에 붙으므로 페이지에 오른쪽 여백을 주지 않는다.

**미션·마이크는 한 덩어리로 바닥에 붙인다.** 디자인 좌표를 그대로 재현하려고
`marginTop: 'auto'` + 바닥 여백 23 + 둘 사이 10 으로 잡았다. 미션이 있든 없든
마이크 위치가 시안과 정확히 맞는다(미션 있음 y357, 없음 y481 — 둘 다 계산이 떨어진다).

**말풍선 최대 폭을 520 → 261 로 줄였다.** 대화 열이 944 에서 497 로 좁아졌다.

---

**세 시안은 한 화면의 세 순간이라 하나로 합쳤다.** `StoryPlayScreen` 이
마이크 상태(`blocked` → `ready` → `listening` → `processing`)로 장면을 굴린다.
장면에 미션이 있으면 미션 패널이 하나 더 붙는다.
그 장면은 왼쪽 뱃지도 "이야기 줄거리" → "이야기 상황" 으로 바뀐다 — 디자인이 그렇다.

**말풍선은 임의 위치가 아니었다.** 시안을 열어보니 배경 위 절대좌표가 아니라
평범한 채팅 레이아웃이다(등장인물 왼쪽 정렬, 아이 오른쪽 정렬). 배경 그림은
불투명도 40%(디자인 실측)로 깔고, 글은 전부 불투명한 판 위에만 올라간다.
**`Screen` 의 `backdrop` prop 은 이제 이 화면에서 쓰지 않는다** — 그림이 화면 전체가
아니라 오른쪽 열 안에만 들어가므로 열 안에 직접 깐다.

**시안의 대본을 그대로 옮기지 않았다.** 디자인의 이야기("방귀뀌는 며느리")와 본문이
서비스에 쓸 수 없는 자리표시 문장이다. 화면 구조와 3/8 진행도는 디자인대로 두고,
문장만 아기돼지 삼형제로 8장면을 새로 썼었다.
**지금은 그 목데이터를 지웠다** — 장면 본문은 서버(`/progress`)가 내려준다.

**"아직 말할 차례가 아니예요" → "아니에요" 로 고쳤다.** 디자인 맞춤법 오류인데
아이가 글을 배우는 앱이라 그대로 두지 않았다. 디자이너 확인 필요.

**배경 그림은 장면별이 아니라 하나뿐이다.** 시안에 들어 있던 한옥 마당 이미지를
`assets/scenes/hanok-yard.jpg` (1536×1055, JPEG 1MB)로 내보내 모든 장면에 쓴다.
원본 PNG 는 6.4MB 라 저장소에 넣지 않았다. 실제로는 서버가 장면마다 내려줄 값이다.

**나레이션 → 아이 차례 전환은 2.5초 타이머다.** TTS 가 붙으면 재생 완료 콜백으로
바꾼다. 마이크를 눌러 녹음을 멈추면 STT 대신 대본에 적힌 예시 답변이 0.9초 뒤에
받아써진 것으로 친다. 둘 다 `StoryPlayScreen` 상단에 TODO 로 표시해뒀다.

**보내기 버튼은 디자인(h42)보다 크다.** `Button size="lg"`(h48)를 썼다 — 아이
손가락이 닿는 버튼이라 터치 권장치(48dp)를 지키는 쪽을 골랐다.

**시안에 있던 세 점 로딩 아이콘 하나를 뺐다.** 161:1158 의 아이 말풍선 왼쪽에
"너의 말을 듣고 있어"(점 3개 + 문구)와 별도의 `three-dots-loading` 이 둘 다 있는데,
같은 뜻이 두 번 나오고 미션 시안(216:277)에는 문구 쪽만 있다. 문구 쪽만 남겼다.

## 활동 화면에서 내린 판단들

**마이크 상태는 `86:448` 가이드가 기준이다.** 도입·대화 시안이 같은 슬롯의 라벨을
11.3px 과 16px 으로 서로 다르게 그려놨는데, 컴포넌트 스펙 프레임이 15/SemiBold 로
정의하고 있어 그쪽을 따랐다.

**정답을 맞히면 카드를 고른 순서대로 다시 깔아준다.** 디자인(92:1215)도 그렇다 —
흩어진 자리에 번호만 붙어 있으면 "이야기가 이런 순서였구나" 가 안 보인다.

**작은 카드(활동 2/2)의 그림은 디자인에 없어서 넣었다.** 시안은 빈 색면인데,
아이가 카드를 보고 이야기를 떠올리는 게 이 활동의 목적이라 큰 카드와 같은 그림을
44px 로 넣었다.

**활동 2/2 캐릭터 판의 그라디언트는 단색으로 뒀다.** `#FFF8F0` → `#FFEEC2` 세로
그라디언트인데, 이것 하나 때문에 `expo-linear-gradient` 를 새로 넣을 만하지 않다고
봤다. 밝은 쪽 단색(`surfaceAccentWarm`)을 쓴다.

**"홈으로 돌아가기" 는 디자인의 회색 채움 대신 앱의 secondary 버튼(흰 배경 + 테두리)
을 썼다.** 이 프레임도 Arial 라벨이 섞인 미완성 시안이고, 모달들의 보조 버튼과
모양을 맞추는 쪽이 낫다고 판단했다.

**작은 버튼들은 디자인보다 크다.** 보내기(h42)·완료(h38) 모두 `size="lg"`(h48)로
올렸다. 아이 손가락이 닿는 버튼이라 터치 권장치를 지키는 쪽을 택했다.

**활동 카드·핵심 단어·다시 말한 문장은 전부 목데이터다.**
`features/story/model/activity.ts` 한 곳에 모여 있다. 카드 그림 5개는 시안에서
그대로 내보내 `assets/cards/` 에 넣었다.

## 보호자 리포트에서 내린 판단들

**세 프레임은 한 화면이다.** `252:193`(위쪽 + 어휘 탭) · `252:249`(표현·논리 탭
내용) · `252:297`(대표 발화 · 집에서 이어가기 · 리포트 이동)을 `ReportScreen`
하나로 합쳤다. `252:297` 의 폭(내용 959)이 `252:193` 의 스크롤 영역 폭과 정확히
같아서, 768 안에 안 들어가는 부분을 따로 그려둔 것이라고 봤다.

**어휘/표현/논리는 진짜 탭이다.** `252:249` 가 표현과 논리를 한 프레임에 세로로
쌓아 그려놔서 연속 스크롤처럼 보이지만, `252:193` 에 알약 탭 세 개가 어휘만 선택된
상태로 그려져 있다. 캔버스를 아끼려고 두 탭 내용을 나란히 그린 것으로 보고 탭으로
구현했다. 탭이라면 한 번에 하나만 보인다.

**위쪽은 고정, 탭 내용부터 아래만 스크롤한다.** 디자인의 `284:210` 이 overflow-clip
이고 그 안에 스크롤바(`252:415`)까지 그려져 있다. 헤더·이야기 줄·말하기 특징·탭은
고정이고 그 아래만 움직인다. 1024×768 에서 읽는 창이 266px 로 좁지만, 탭을 눌러
비교하는 화면이라 탭이 스크롤을 따라 올라가면 더 쓰기 어렵다.

**화면 바탕이 이 화면만 흰색이다.** 디자인이 `bg-white` 로 그려져 있고, 안쪽
"어휘 피드백"·"선정 이유" 블록이 `#F8F9FA`(앱의 기본 페이지 배경)라 바탕까지 같은
색이면 그 블록이 사라진다. `Screen` 에 `background` prop 을 새로 넣어 이 화면만
`surface` 로 준다.

**그 대가로 아이 전환 알약이 사라졌었다.** `ChildSwitcher` 는 "회색 페이지 위 흰
알약"으로 색을 뒤집어 둔 상태였는데(홈 기준), 이 화면은 바탕이 흰색이라 흰 알약이
그대로 묻혔다. `tone` prop 을 추가해 **바탕이 흰 화면에서는 디자인 원본대로
`#F8F9FA` 알약**을 쓰게 했다. 앞으로 `background="surface"` 인 화면을 만들면
그 위에 올리는 흰색 요소를 전부 다시 봐야 한다.

**이전/다음 리포트 버튼은 앱의 primary·secondary 로 맞췄다.** 디자인은 둘 다 주황
채움인데 이전이 `#FFA833`, 다음이 `#FF9200` 이고 라벨이 Arial 이다 — 홈 "이어하기"
때와 같은 미완성 시안 패턴이다. 모달·완료 화면의 보조 버튼과 모양을 맞춰 이전은
secondary(흰 배경 + 테두리)로 뒀다.

**대신 비활성 색은 디자인(287:273)을 따랐다.** `Button` 의 primary 비활성은 옅은
주황(`#FFC266`)인데, 이건 로그인의 "계속하기"처럼 입력만 채우면 눌리는 버튼 기준이다.
리포트 페이저는 끝에 도달하면 정말로 못 누르는 버튼이라 주황으로 두면 계속 누르게
된다. `Button` 에 `disabledTone="neutral"` 을 새로 넣어 회색으로 내렸다.

(시안 본문이 자리표시라 목데이터를 따로 썼었는데, **지금은 그 목데이터를 지웠다** —
리포트 본문은 서버가 만들어 내려준다.)

**이야기 줄의 아이콘이 사람 모양이다.** 이야기 제목 옆인데 하단 탭 '마이페이지'와
같은 벡터를 두 가지 색으로 칠한 것이다(`252:204`). 이야기 그림으로 바꾸고 싶었지만
에셋을 임의로 바꾸지 않는다는 규칙대로 시안 그대로 내보내 썼다
(`assets/icons/profile-fill.svg`). **마이페이지의 집 모양 아이콘과 같은 종류의
디자인 오류로 보인다 — 디자이너 확인 필요.**

**"궁금해한 어휘" 칩이 파란 배경에 갈색 글자다**(`#E7F5FF` + `#4D2500`).
주황 칩에서 글자색만 안 바꾼 것처럼 보이는데, 대비 자체는 충분해서 시안대로 뒀다.
`colors.surfaceInfo` / `colors.textInfo` 로 토큰화했다. 이것도 확인 대상이다.

**디자인에 없어서 직접 처리한 것.** 어휘 칩이 한 줄을 넘치면 다음 줄로 내려가게
했고(시안엔 한 줄뿐), 표현·논리 카드의 인용문은 고정 높이 36 대신 세로 여백으로
바꿔 아이 말이 길어도 잘리지 않게 했다. 이야기 제목은 말줄임 처리했다.

## 알아둬야 할 판단들

**"미완성 시안"이 섞여 있다.** 로그인·이야기 목록처럼 자동레이아웃 + Pretendard +
Variables 로 제대로 짜인 화면이 있고, 모달 몇 개는 절대좌표 + Inter/Arial +
소수점 폰트크기(13.1px 등)로 된 시안이다. 후자는 사용자 승인을 받아 **토큰으로
보정**했다(Pretendard 로 바꾸고 크기를 정수화). 그쪽 디자인이 갱신되면 다시 맞춰야 한다.

**디자인 오류로 보이는 것.** 마이페이지 "학습 리포트 보기" 아이콘이 집 모양이다
(하단 탭 '홈'과 같은 에셋). 디자인 자체가 그러해서 그대로 구현했다. 디자이너 확인 필요.

**빈 상태·긴 텍스트·다중 항목 처리는 디자인에 없어서 직접 만들었다.**
`EmptyState` 문구, 아바타 줄바꿈, 이름 말줄임 등. 시안이 나오면 교체 대상이다.

**리포트·계정 설정에만 보호자 확인을 걸었다.** 어느 메뉴에 걸지는 명시가 없었다.
도움말은 열어뒀다. 리포트는 이제 확인을 통과하면 `/report` 로 실제로 넘어간다
(계정 설정은 아직 갈 화면이 없다).

## 화면을 점검할 때 봤던 것들

(기본 검증 루프는 아래 [검증 방법](#검증-방법) 에 정리해뒀다. 여기는 과거에
전수 점검을 돌리면서 실제로 결함이 나왔던 항목들이다 — 같은 걸 또 보면 된다.)

### 전체 점검 한 번 돌린 결과 (2026-08-09)

퍼블리싱한 화면을 한 번에 훑을 때 아래를 봤다. 다음에도 같은 걸 보면 된다.

- 전 라우트(17개)를 `history.pushState` 로 순회하며 렌더 여부·가로 넘침·버튼 수
- 버튼마다 **중첩 여부**와 **접근성 이름**. 눌림 영역이 겹치면 스크린리더가 두 개를
  겹쳐 읽고 웹에서는 `button` 안 `button` 이 된다 (단어장 카드에서 실제로 나왔다)
- 없는 id(`/story/999/...`)와 대본 없는 이야기(`/story/1/...`)로 들어가 보기 —
  막다른 길이 생기기 쉽다
- 1280×800 / 1024×768 / 768dp 세 폭에서 가로 넘침
- `grep` 으로 하드코딩 색·폰트, feature 간 import, 미사용 에셋·토큰

이때 고친 것: 단어장 카드의 중첩 눌림 영역, 활동 2/2 빈 상태의 뒤로가기 부재,
완료 화면이 대본 없는 이야기에도 "0번·0개" 축하를 띄우던 것, 카테고리 칩 터치 영역,
`rgba` 리터럴 3개를 토큰으로.

### 리포트 화면 점검 (2026-08-11)

같은 항목을 리포트에도 돌렸다. 세 폭 모두 가로 넘침 0, 중첩 눌림 영역 0,
이름 없는 버튼 0, 콘솔 오류 없음. 타이포는 `getComputedStyle` 로 재서 시안과
대조했다(제목 24 Bold / 이야기 17 SemiBold / 특징 16 Bold `#E07D00` / 대표 발화
20 Bold / 주제 14 Regular, 전부 Pretendard).

마이페이지 → 학습 리포트 보기 → 보호자 확인 → `/report` 까지 실제로 눌러 확인했고,
탭 전환·리포트 앞뒤 이동·양 끝 비활성까지 동작한다.

**주의**: `.expo/types/router.d.ts` 는 dev 서버가 만드는 캐시라, 새 라우트를 추가한
직후에는 `npm run typecheck` 이 "not assignable to parameter of type ..." 로 깨진다.
코드 문제가 아니다. 서버를 한 번 띄우면(`npm run web`) 타입이 다시 생성된다.

### 전체 화면 ↔ 디자인 수치 대조 (2026-08-11)

"구현하면서 디자인과 벌어졌을 것"이라는 지적을 받고 퍼블리싱한 화면 전부를
Figma 현재 지오메트리와 1:1로 다시 맞췄다.

먼저 **디자인이 바뀌었는지**를 봤다. `182:303` 전체를 훑어 `234:567`(직전 작업의
최대 노드) 이후에 생긴 노드를 뽑으니 전부 보호자 리포트와 그 래퍼 섹션뿐이었다.
즉 **기존 화면의 시안은 그대로**고, 벌어진 건 구현 쪽이었다.

찾아서 고친 것 6건:

| 무엇                       | 디자인    | 고치기 전       | 원인                                                                 |
| -------------------------- | --------- | --------------- | -------------------------------------------------------------------- |
| 페이지 좌우 여백 (전 화면) | 24        | 20              | `Screen` 의 `select` 에 `expanded` 키가 없어 `medium`(20)으로 떨어짐 |
| 단어장 단어 카드 높이      | 279       | 152 (내용 높이) | 줄에 높이를 안 잡아 카드가 납작해짐                                  |
| 이야기 목록 본문 여백      | 48        | 20              | 이 화면만 본문이 한 겹 더 들어가 있는 걸 놓침                        |
| 홈 좌우 칸 비율            | 484 : 444 | 468 : 468       | 양쪽 `flex: 1` 로 균등 분할                                          |
| 이야기 상세 시작 버튼 높이 | 56        | 48              | `Button size="lg"` 를 그대로 씀                                      |
| 마이페이지 아이 카드 간격  | 12        | 16              | `spacing.xl` 을 씀                                                   |

**비율은 `flex` 에 실측값을 그대로 넣는다.** 홈은 `flex: 484` / `flex: 444`,
이야기 상세는 `flex: 524` / `flex: 428`. `1.2` 같은 어림수를 쓰면 근거가 사라진다.

고친 뒤 다시 재서 **디자인과 좌표까지 일치**하는 걸 확인했다 (1024×768 기준):

- 이야기 상세 썸네일 524×402 @x24, 시작 버튼 428×56 @x572
- 홈 두 칸 484 @x24 / 444 @x556
- 마이페이지 아이 카드 317×66 @x24, 간격 12
- 활동 카드 160×200 @x84 간격 14, 대화 패널 944 @x40, 완료 버튼 200×52 @x306·x518
  (이 셋은 원래부터 맞았고, 여백을 고쳐도 가운데 정렬이라 그대로다)

**측정할 때 등장 애니메이션을 조심할 것.** `Appear` 의 `from="scale"` 이 끝나기 전에
재면 폭이 0.92배로 나온다. 실제로 회원가입·아이생성 카드가 386/570 으로 나와
어긋난 줄 알았는데, 2.5초 기다렸다 다시 재니 420/620 으로 정확했다. **최소 2초는
기다린 뒤 잰다.**

**웹에서 잰 값은 세로 스크롤바(약 15px)만큼 좁게 나온다.** 안드로이드는 스크롤바가
겹쳐 그려져서 이 차이가 없다. 이야기 목록 카드가 웹에서 294, 기기에서 299 인 이유다.

**남겨둔 것**: 1280 처럼 1024 보다 넓은 화면에서는 본문이 976 이 아니라 1024 로
잡힌다 (`Screen` 의 `maxContentWidth` 가 본문 폭이고 여백은 그 바깥에 붙는다).
1280 에서는 좌우에 128px 씩 배경이 남아 여백이 충분해 그대로 뒀다.

### 태블릿 실기기에서만 나온 결함 2건 (2026-08-11)

위 수치 대조는 전부 웹(`npm run web`)에서 했는데, **에뮬레이터에 올리니 웹에서는
드러나지 않는 게 두 개 더 나왔다.** 수치만 재고 끝내지 말 것.

**1. 하단 탭 바만 화면 끝까지 늘어났다.** 본문은 `maxContentWidth`(1024)로 묶이는데
`(tabs)/_layout.tsx` 의 탭 바에는 상한이 없어서, 1280dp 태블릿에서 탭 바만 1232dp 로
퍼졌다. 같은 화면의 본문(1024)보다 208dp 넓다. 리포트는 탭 바를 화면 안에서 직접
그려 1024 였기 때문에 **화면끼리 탭 바 폭이 달랐다.** 탭 바에도 같은 상한을 걸었다.
1024 폭에서는 둘 다 976 이라 웹에서는 절대 안 보인다.

**2. 보호자 확인 모달이 키보드에 통째로 가렸다.** 인풋과 취소·확인 버튼이 전부
키보드 뒤로 들어가 아무것도 못 눌렀다. `Screen` 은 `useKeyboardInset` 으로 처리하고
있었지만 `Modal` 은 빠져 있었다. 모달 배경에 같은 인셋을 줘서 카드를 위로 올렸다.
**입력이 있는 모달은 `Screen` 규칙(AGENTS.md)이 적용되지 않는다는 걸 놓쳤던 것이다.**

### 에뮬레이터를 하나 더 띄울 때 (다른 프로젝트와 같이 쓸 때)

다른 에뮬레이터가 이미 돌고 있으면 아래를 지킨다. 실제로 한 번 겪었다.

- `emulator -port 5556` 으로 포트를 직접 지정하고, **모든 adb 명령에 `-s emulator-5556`
  을 붙인다.** 안 붙이면 상대 기기로 간다. `adb kill-server` 는 절대 쓰지 않는다.
- Metro 포트가 겹치는지 `netstat` 으로 먼저 본다. `adb reverse` 는 기기별이라
  서로 다른 호스트 포트를 봐도 충돌하지 않는다.
- **`-no-snapshot-save` 로 띄운다.** 종료할 때 새 상태를 쓰지 않는다. 다만 이걸
  줘도 **실행하는 순간 그 AVD 의 기존 퀵부트 스냅샷은 무효화된다**(`snapshot.pb` 가
  11바이트로 잘린다). 남의 AVD 를 빌려 쓰면 다음번에 콜드 부팅하게 되니, 가능하면
  전용 AVD 를 새로 만들어 쓰는 게 낫다. 설치한 앱도 `userdata-qemu.img.qcow2` 에
  그대로 남으므로 끝나면 `adb -s <serial> uninstall` 로 지운다.
- **좌표로 탭하기 전에 반드시 스크린샷을 먼저 찍어 우리 앱이 전면인지 확인한다.**
  다른 앱의 잔여 태스크(설정 화면 등)가 앞에 떠 있을 수 있다.
- 딥링크는 앱이 이미 전면일 때는 라우팅되지 않는다. 화면 안의 탭 바를 누르거나,
  `am force-stop` 후 딥링크로 다시 띄운다.
- 화면 확인은 `adb -s <serial> exec-out screencap -p > a.png`.


---

# 백엔드 연동 (2026-08-17 마감 현재)

여기서부터가 지금 하고 있는 작업이다. 위쪽(디자인 구현)은 다 끝난 이야기다.

## 한 줄 요약

**앱은 전 흐름이 돈다. 남은 건 서버 데이터다 — 대화 장면이 안 열린다(1-1).**

## 지금 상태

| | |
| --- | --- |
| 화면 | 디자인 40개 전부 구현 |
| API | 명세 29개 중 28개 연결 (`GET /users/mypage` 만 일부러 제외) |
| 목데이터 | 없다 |
| 서버 | 살아 있다. `/main` 도 200 |
| 로그인 · 아이 · 이야기 · 활동 · 다시 말하기 · 완료 · 홈 · 단어장 | **된다 (08-17 실기기 확인)** |
| 대화(마이크·씬 단어·미션) | **서버 데이터 때문에 화면이 안 열린다 (1-1)** |
| 리포트 | **아직 못 봤다** — 보호자 확인이 계정 비밀번호를 요구한다 |

---

# 1. 지금 막혀 있는 것 (백엔드)

> **08-17 에 전 흐름을 실기기로 끝까지 돌렸다.** 로그인부터 완료 화면까지 이어진다.
> `/main` 404 와 사진 객체 키는 **서버에서 해결됐다.** 대신 훨씬 큰 게 하나 나왔다.

## 1-1. ⚠️ 대화가 통째로 열리지 않는다 — 모든 장면이 `kind: narration`

**이 앱에서 아이가 말을 하는 화면이 하나도 열리지 않는다.** 대화 앱인데 대화가 없다.

9장면을 전부 찍어봤다 (`방귀 뀌는 며느리`, 08-17).

| step | kind | character_name | mission | vocabularies |
| --- | --- | --- | --- | --- |
| 1~9 **전부** | `narration` | 3·5·7·9 는 있다 | 전부 `null` | 전부 `0개` |

등장인물이 아이 이름을 부르며 말을 거는 장면(3·5·7·9)도 `kind` 가 `narration` 이다.
앱은 `narration` 이면 마이크를 잠그고 "아직 말할 차례가 아니에요"만 띄운다
(`StoryPlayScreen`). 그래서

- **마이크가 한 번도 안 열린다** → `POST .../speak` 를 한 번도 못 탄다
- 씬 단어 고르기가 안 나온다 (`vocabularies` 가 전부 0개)
- 미션 패널이 안 나온다 (`mission` 이 전부 null)
- `max_turns` / `turn` 도 전부 null

**결과가 완료 화면에 그대로 찍힌다 — 발화 횟수 0번 · 새로 배운 단어 0개.**
단어장도 계속 빈 상태다.

### UI 문제가 아니라는 걸 서버에 직접 물어서 확인했다

"마이크가 안 열리는 게 화면 문제 아니냐"를 가르려고, 등장인물이 붙은 장면(3/9)을
**억지로 대화 장면으로 취급**하게 바꿔 마이크를 열고 실제로 녹음해 보냈다.

```
POST /progress/{story}/steps/3/speak → 400 {"detail":"이 장면에서는 말할 수 없습니다."}
```

**서버가 그 장면의 발화를 거부한다.** 화면을 어떻게 고쳐도 아이는 말할 수 없다.
(실험 코드는 되돌렸다.)

**앱은 고칠 게 없다.** 인수인계 1-9 에 적어둔 "`kind` 모순"이 실제로 터진 것이다 —
명세 한쪽은 `mission` 이 kind 라 하고 다른 쪽은 아니라 한다. **백엔드가 대화 장면에
`kind: dialogue` 를 주고 `vocabularies` 를 채워야 한다.**

> ### 씬 단어 고르기는 **다 돼 있다. 손대지 말 것** (08-17 사용자 확인)
>
> "모르는 단어가 아직 없다"는 이야기가 두 번 나와서 확인했다. **기능 범위는
> 시안 그대로가 맞고**(`417:602` 접힘 · `417:616` 펼침 — 낱말 + 체크박스가 전부이고,
> 뜻을 보여주는 화면은 이 흐름에 없다), 앱에는 `SceneWordPicker` 와 저장
> API(`POST .../vocabularies`)가 이미 붙어 있다. `kind` 와 무관하게 **단어가 하나라도
> 오면 그려진다** (0개면 컴포넌트가 `null` 을 반환한다).
>
> 08-17 저녁에 1~7 장면을 다시 찍어봐도 전부 `vocabularies: []` 였다.
>
> **임시로 앱이 낱말을 지어내 띄우는 건 사용자가 안 하기로 했다.** 체크를 저장하려면
> 서버가 준 `scene_vocabulary_id` 가 필요해서, 지어낸 낱말은 화면에만 보이고
> 단어장·리포트에는 못 쌓인다 — 되레 "되는 것처럼 보이는" 상태가 된다.
>
> **서버가 채우면 그날 바로 뜬다.**

### 그동안 아이가 말은 할 수 있게 해뒀다 (임시, 3-6)

사용자 결정으로 **등장인물이 있는 장면에는 마이크를 띄우고, 앱이 직접 받아써서
말풍선에 보여준다.** 서버로는 아무것도 안 보내므로 **리포트·단어장에는 안 쌓인다.**
`kind: dialogue` 가 오기 시작하면 서버 경로로 저절로 돌아간다.

## 1-2. 서버 대사의 호격 조사가 틀렸다

```
김철수아, 내 방귀가 너무 크다는 걸…      ← 받침이 없으니 "김철수야"
김철수이 덕분에 …                      ← "김철수 덕분에"
```

받침 여부와 상관없이 조사를 붙이고 있다. **아이가 글을 배우는 앱이라 그냥 둘 수 없다.**
앱은 같은 계산을 이미 한다(홈 인사말이 "안녕, 김철수야!" 로 맞게 나온다 —
`HomeScreen` 의 `vocativeParticle`). 서버도 같은 규칙을 쓰면 된다.

## 1-3. 순서 맞추기 정답 응답에 핵심 단어가 없다

정답을 맞혔는데도 `vocabulary` 가 빈 배열이다 (08-17 저녁 실기기 응답 그대로).

```json
{"is_correct":true,"attempt_count":1,"vocabulary":[]}
```

그래서 정답 화면과 다시 말하기 화면에 **"핵심 단어" 제목만 남고 칩이 하나도 없다.**
1-1 과 같은 뿌리다 — 장면에 단어가 하나도 안 실려 있다.

## 1-4. 진행 중이던 세션이 조용히 리셋된다

이야기 상세에서 **시작하기를 다시 누르면 409 가 아니라 `1/9` 로 새로 시작한다.**
활동 단계까지 갔던 세션이 그대로 날아갔다. 명세는 "아이당 `in_progress` 는 하나"라
같은 이야기를 다시 시작하면 어떻게 되는지가 정의돼 있지 않다. **이어보기로 할지
새로 시작으로 할지 정해야 한다** — 지금은 아이가 앱을 잠깐 나갔다 오면 처음부터다.

## 1-5. `/main` 에 완료 기록이 없다

이야기를 끝까지 마쳐도 홈에 아무 흔적이 안 남는다. 응답에 **그런 필드가 없다.**

```json
{"continue_story": null, "recommended_stories": [ ... ]}
```

`continue_story` 는 *읽던* 이야기라 완료하면 null 이 되는 게 맞다. 즉 지금 스키마로는
"이만큼 읽었다"를 홈에 보여줄 방법이 없다. **홈 시안(78:184)에도 완료 기록 영역은
없으므로**, 필요하면 디자인과 API 를 같이 정해야 한다.

## 1-6. 이미지 필드가 두 가지 모양으로 온다 ⚠️

같은 `image_url` 이 이렇게 온다.

```
19:25  step 1  "scenes/banggui/scene_01.png"           객체 키 → 화면이 빈 칸
19:47  step 1  "https://…/scene_01.png?X-Amz-Signature=…"  presigned → 정상
```

**스킴 없는 값을 `<Image>` 에 넣으면 RN 은 에러도 없이 아무것도 안 그린다.**
logcat 에 흔적이 없어서 원인을 찾기 어렵다 (화면 픽셀을 재서야 확인했다 — 3장 참고).

- **프로필 이미지는 presigning 이 아예 없다**: `https://<버킷>.s3.ap-northeast-2.amazonaws.com/avatar:bear`
  → `curl` 로 **403 AccessDenied**. 장면 그림은 `s3.amazonaws.com`(리전 없음)을 쓰는 걸
  보면 **코드 경로가 다르다**
- 이야기 썸네일은 `thumbnail_url: null` — 데이터 자체가 없다

앱에는 08-17 에 가드를 넣었다(3-4) — 못 받을 값이면 대체 그림으로 떨어진다.
**서버가 전부 presigned 로 통일하는 게 근본 해결이다.**

## 1-7. 해결된 것 (기록용)

| 전 | 지금 |
| --- | --- |
| `GET /main` 404 (홈이 통째로 안 뜸) | **200. 홈이 뜬다** (추천 이야기·이어하기 카드 정상) |
| 사진이 객체 키로 옴 | **공개 URL 로 온다.** 다만 그 바람에 아바타가 깨졌다 ↓ |

**사진 URL 이 고쳐지면서 아바타가 깨졌다.** 서버가 `profile_image_url` 을 객체 키로
취급해 S3 주소를 앞에 붙여 돌려준다 — 우리 아바타 표식까지 그렇게 된다.

```
보낸 값  avatar:bear
받은 값  https://<버킷>.s3.ap-northeast-2.amazonaws.com/avatar:bear
```

앱이 그걸 사진으로 보고 `<Image>` 에 넣어 **아바타를 고른 모든 아이가 빈 원**이 됐다.
08-17 에 앱에서 우회했다(주소 마지막 조각이 표식이면 아바타로 되돌린다).
**백엔드에 `avatar_id` 필드가 생기면 그 우회를 통째로 지운다.**

## 1-8. `topics` 가 앱의 카테고리와 하나도 안 겹친다

**08-17 실기기에서도 그대로다** — 목록의 유일한 이야기에 `다름` 칩만 붙어 있고,
상세에는 `다름 · 자기이해 · 장점 발견` 셋이 붙는다.

서버가 주는 값:

```json
"topics": ["다름", "자기이해", "장점 발견"]
```

앱의 카테고리 칩(디자인 `125:115`): `전체 · 우정 · 용기 · 감정 · 상상 · 모험`

**겹치는 게 없어서 카테고리 필터가 사실상 아무것도 못 거른다.** 게다가 `topic=우정` 을
보내도 서버가 무관한 이야기를 그대로 돌려줬다 — **필터 자체가 안 먹는 것으로 보인다.**
디자인의 칩 목록을 바꿀지, 서버 topics 를 맞출지 결정이 필요하다.

## 1-9. 백엔드에 물어볼 것 (누적)

**⚠️ 위의 세 줄이 지금 가장 급하다** — 대화가 안 열리는 것(1-1), 조사(1-2),
핵심 단어 빈 응답(1-3). 나머지는 그다음이다.

| 항목 | 내용 |
| --- | --- |
| **`kind` 가 전부 narration** | **대화·미션·씬 단어가 통째로 안 열린다 (1-1). 최우선** |
| **호격 조사** | `김철수아` / `김철수이` — 받침을 안 본다 (1-2) |
| **`vocabulary` 빈 응답** | 정답을 맞혀도 핵심 단어가 안 온다 (1-3) |
| 같은 이야기 재시작 | 409 없이 1/9 로 리셋된다. 이어보기가 맞는지 (1-4) |
| **홈에 완료 기록 없음** | `/main` 응답에 그런 필드가 없다. 디자인과 같이 정해야 한다 (1-5) |
| **리포트 목록 API** | 없어서 "가장 최근에 끝낸 이야기"를 물어볼 데가 없다 (3-7) |
| **이미지가 키/URL 섞여 온다** | 프로필은 presigning 자체가 없어 403 (1-6) |
| `avatar_id` 필드 | 생기면 아바타 우회(1-5)를 지운다 |
| 아이 삭제 API | 명세에 없다. 테스트로 만든 아이를 지울 방법이 없다 |
| `GET /stories/{id}` | 이야기 상세(줄거리·역할 안내·등장 인물)를 받을 데가 없다 |
| 리텔링 STT | `retell` 이 음성 파일을 받게 해줄지 (4-2) |
| 리포트 목록 API | 생기면 `StoryPicker` 를 되살린다 |
| 401 문구 | "토큰이 틀림"과 "프로필 없음"이 같은 문구로 나가면 또 헤맨다 |
| 응답 감싸기 | `/stories` 가 `{items,total,limit,offset}` 로 온다. 다른 목록도 그런지 |

---

# 2. 다음 세션이 할 일 — 테스트 위주

**서버가 살아났으므로 이제 실제로 눌러보며 버그와 미구현을 찾는다.**

## 2-1. 전체 흐름 훑기 — 08-17 에 끝까지 갔다

```
로그인 → 아이 선택 → 이야기 목록 → 상세 → 대화 9장면 → 순서 맞추기(정답)
      → 다시 말하기(STT) → 완료 → 홈 → 단어장          [전부 통과]
                                        리포트 → 보호자 비밀번호가 필요해 못 봤다
```

**확인된 것**

| | |
| --- | --- |
| 세션 시작·장면 진행 | 9장면 전부 정상. 나레이션 글·배경 그림 다 온다 |
| TTS | Samsung TTS 에 붙어 실제로 읽는다 (소리는 미확인, 연결 로그로 확인) |
| 순서 맞추기 | 서버 채점 정상. 정답이면 맞춘 순서로 다시 깔린다 |
| 다시 말하기 STT | **실기기에서 실제로 받아쓴다** (주변 소음까지 잡았다) |
| 완료 화면 | 제목·수치 정상 (수치가 0인 건 1-1 탓이다) |
| 홈 | `/main` 200. 추천 이야기·빈 이어하기 카드 정상 |
| 단어장 | 빈 상태 정상 |

**여기서 나온 결함은 [1장](#1-지금-막혀-있는-것-백엔드)(서버)과 [3장](#3-08-17-에-고친-것)(앱)에 있다.**

**아직 못 본 것**

1. **리포트 화면** — 보호자 확인이 계정 비밀번호를 요구해서 대신 넣지 않았다.
   생성·폴링·탭 전환이 실데이터로는 미확인이다
2. **대화 화면의 마이크·씬 단어·미션** — 1-1 때문에 화면 자체가 안 열린다.
   백엔드가 `kind: dialogue` 를 주면 그때 처음 보게 된다
3. `GET /progress/active` 의 실제 응답 모양 (명세에 예시가 없어 추측으로 짜놨다).
   시작하기가 매번 새 세션을 여는 지금은 이 경로를 안 탄다 (1-4)
4. 회원탈퇴 · 소셜 로그인 (2-3)

## 2-2. 오류·빈 상태를 일부러 만들어 본다

서버가 오래 죽어 있어서 여태 "잘 된 경우"만 봤고, **오류 상태에서 실제로 결함이 나왔다**
(4-6). 다른 화면에도 같은 종류가 있을 가능성이 높다.

- 목록이 비었을 때 / 불러오기 실패했을 때 / 아이가 없을 때
- **본 화면 위에 오류 문구만 얹으면 앞뒤가 안 맞는 화면이 된다.** 안내와 할 일만 남긴다
- `EmptyState` 는 `flex: 1` + 세로 여백 `5xl` 이라 자리를 늘려 주변을 밀어낸다
- `Button` 은 `alignSelf: 'flex-start'` 라 부모의 가운데 정렬을 덮어쓴다

## 2-3. 아직 구현 안 된 것

| 항목 | 상태 |
| --- | --- |
| **소셜 로그인** | `LoginScreen` 의 `handleSocialPress` 가 TODO. 콘솔 확인 필요(아래) |
| **계정 설정 화면** | 디자인 준비 중. `PATCH /users/me`(`useUpdateParentProfile`)는 이미 있다 |
| **도움말 화면** | 디자인 준비 중 |
| **이야기 상세 3블록** | 줄거리·역할 안내·등장 인물. `GET /stories/{id}` 필요 |
| **리포트 이야기 드롭다운** | 리포트 목록 API 필요. `StoryPicker` 컴포넌트는 남아 있다 |
| **회원탈퇴** | 붙였지만 한 번도 안 눌러봤다. 되돌릴 수 없으니 테스트 계정을 하나 더 만든다 |

## 2-4. 401 자동 복구 — 넣었다 (2026-08-17). 실기기 확인만 남았다

08-16 에는 일부러 미뤘다 (백엔드가 401 을 주는 상태에서 넣으면 로그인하자마자
튕겨나가 디버깅이 불가능했다). 401 이 풀려서 넣었다.

```
401 → 토큰 갱신 1회 → 성공하면 새 토큰으로 그 요청 재시도
                    → 되살릴 수 없으면 signOut(local) → AuthGate 가 로그인 화면으로
                    → 네트워크 문제로 갱신이 실패한 것뿐이면 로그아웃하지 않는다
```

`apiClient` 응답 인터셉터(`retryAfterRefresh`)와 `supabase.ts` 의
`setAuthTokenRefresher` 두 곳이다. "언제 로그아웃시킬지" 는 등록하는 쪽이 정한다 —
`client.ts` 가 supabase 를 직접 부르면 순환 참조다.

**명세와 다르게 한 것 하나.** *재시도가 또 401 이면 로그아웃시키지 않고 그대로
실패시킨다.* 토큰은 멀쩡한데 **서버에 프로필 행이 없을 때도 401** 이 오는데(같은
상태코드다), 그걸 토큰 문제로 보고 로그아웃시키면 사용자는 이유도 모른 채 튕긴다.
백엔드에 401 문구를 갈라달라고 하면(1-7) 그때 프로필 없음은 `sync-profile` 한 번
부르고 재시도하는 lazy repair 로 나눌 수 있다.

**확인한 것** — 실제 인터셉터를 Node 로 불러 adapter 만 가짜로 갈아끼우고 5가지를
돌렸다 (전부 통과):

| | |
| --- | --- |
| 갱신 성공 | 두 번째 요청이 **새 토큰**으로 나가고 응답이 돌아온다 |
| 재시도도 401 | 두 번만 나가고 끝난다 (**무한 반복 없음**) |
| 갱신 실패 | 재시도 없이 401, 세션 정리 호출 |
| 401 이 셋 동시에 | 갱신은 **한 번만** (refresh token 이 연달아 회전하면 뒤쪽이 죽는다) |
| 500 | 손대지 않는다 |

**아직 실기기에서는 못 봤다.** 토큰을 일부러 죽여서 확인하려면 앱을 한 시간 넘게
두거나 Supabase 콘솔에서 세션을 무효화해야 한다(콘솔은 팀원 소유다 — 7장).

## 2-5. 임시 코드 정리

`src/shared/api/client.ts` 의 `[API]` 실패 로그가 아직 남아 있다(6장). 연동 확인이
끝나면 지우거나 `__DEV__` 로 감싼다. **디버깅에 계속 유용하니 급히 지울 필요는 없다.**

## 2-6. 태블릿에 화면 띄우기

```bash
adb devices                      # device 로 잡히는지 먼저
npx expo start --dev-client      # 이미 떠 있으면 새로 띄우지 않는다 (포트 충돌로 죽는다)
adb reverse tcp:8081 tcp:8081    # 태블릿을 뽑았다 꽂으면 반드시 다시 건다
adb shell am force-stop com.goodquestion.app
adb shell am start -a android.intent.action.VIEW \
  -d "exp+good-question-app://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081"
```

콘솔은 사용자가 자기 터미널에서 직접 띄운다.

```bash
adb logcat -T 1 -s ReactNativeJS:V ExpoSpeechService:V
```

딥링크로 특정 화면 열기 — **앱이 전면이면 라우팅되지 않으니 `force-stop` 을 먼저 한다.**

```bash
adb shell am force-stop com.goodquestion.app
adb shell am start -a android.intent.action.VIEW -d "goodquestion://<경로>"
```

| 경로 | 화면 |
| --- | --- |
| `/` · `/signup` · `/find-password` | 로그인 · 회원가입 4단계 · 비밀번호 찾기 3단계 |
| `/child/select` · `/child/create` | 아이 선택 모달 · 아이 등록 (`?id=` 면 수정) |
| `/(tabs)/home` `/story` `/wordbook` `/mypage` | 탭 4개 |
| `/story/[id]` `/play` `/activity` `/retell` `/done` | 이야기 흐름 |
| `/word/[id]` · `/report` · `/dev/gallery` | 단어 상세 · 리포트 · 컴포넌트 갤러리 |

---

# 3. 08-17 에 고친 것

실기기로 전 흐름을 돌리다 나온 것들이다. 서버 쪽 결함은 [1장](#1-지금-막혀-있는-것-백엔드)에 있다.

## 3-1. 아바타를 고른 아이가 전부 빈 원이었다

원인은 [1-7](#1-7-해결된-것-기록용) — 서버가 아바타 표식 앞에 S3 주소를 붙여 돌려준다.
주소 마지막 조각이 표식이면 아바타로 되돌린다 (`avatarRef.ts`).

**진단 방법이 쓸모 있었다.** 원 안쪽 픽셀을 찍어 카드 배경색과 같은 걸 확인해서
"글자 뱃지가 아니라 `<Image>` 분기를 탔다"로 좁혔고, 그다음 서버 값을 로그로 찍었다.
`adb exec-out screencap` + 픽셀 샘플링은 눈으로 보는 것보다 정확하다.

## 3-2. 다시 말하기 카드 번호가 아이가 맞춘 순서와 달랐다

정답을 맞히면 활동 화면은 정답 순서로 다시 깔아주는데, 다음 화면인 다시 말하기는
**서버가 섞어 준 배열**에 1~9 를 붙이고 있었다. 앞 화면이 순서를 라우트 파라미터로
넘긴다 (핵심 단어와 같은 방식).

**서버 카드 배열은 요청마다 다시 섞인다** — 같은 세션에서 두 번 확인했다.

## 3-3. 핵심 단어 구분자가 양쪽에서 어긋나 있었다

보내는 쪽은 U+001F 로 잇고 받는 쪽은 빈 문자열로 쪼개고 있었다. 눈에 안 보이는
문자라 코드만 봐서는 못 찾는다 — `KEYWORD_SEPARATOR` 한 곳으로 모았다.

## 3-4. 받아올 수 없는 이미지 값은 대체 그림으로 떨어뜨린다

원인은 [1-6](#1-6-이미지-필드가-두-가지-모양으로-온다-️). 스킴 없는 값을 `<Image>` 에
넣으면 **RN 은 에러도 없이 아무것도 안 그린다.** `shared/lib/remoteImage` 한 곳으로
규칙을 모으고 장면·썸네일·활동 카드·프로필 네 자리에 적용했다.

**진단이 오래 걸렸던 이유를 남긴다.** logcat 에 이미지 실패가 한 줄도 안 찍혀서,
"서버가 값을 안 준 것"과 "앱이 못 그린 것"이 구분되지 않았다. 결국 화면 픽셀을
재서(UI 없는 띠의 표준편차 7.3 → 0.00) "그림이 아예 없다"를 확인했다.

## 3-5. 읽어주는 동안 마이크·보내기를 감췄다 (말할 타이밍)

**"내가 말할 타이밍이 애매하다"는 지적에서 출발했다.** 나레이션 장면에도 회색
마이크("아직 말할 차례가 아니에요")가 계속 떠 있어 언제가 자기 차례인지 알 수 없었다.

디자인을 다시 보니 그 회색 마이크는 **지금 시안에 없는 상태**였다 — 나레이션
시안(`380:342`)에는 마이크도 보내기도 없고, 마이크 가이드(`86:448`)의 부제가
"대화 화면에서 사용되는 마이크 버튼의 **3가지** 상태" 다. `blocked` 는 삭제된
옛 도입 시안(`86:410`)의 잔재였다.

**규칙: 화면에 뭔가 나타나면 그때가 아이 차례다.**

| | 읽어주는 동안 | 다 읽은 뒤 |
| --- | --- | --- |
| 나레이션 | 아무것도 없음 | 보내기 |
| 대화 | 아무것도 없음 | 마이크 + 보내기 |

`micState = 'blocked'` 로 "읽어주는 중"까지 표현하던 걸 `isReading` 으로 분리하고
`MicState` 를 3개로 줄였다.

**갇히지 않게 안전장치를 두 겹으로 뒀다.** 읽는 동안 누를 것이 하나도 없으므로
읽기가 조용히 실패하면 아이가 빈 화면에 갇힌다.

1. 소리가 시작조차 안 되면 3초 뒤 넘어간다 (소리가 나면 이 타이머는 걷힌다)
2. 시작은 했는데 끝 신호가 안 오면 글자 수에 비례한 상한으로 넘어간다

**예전 안전장치(2.5초 고정)는 새 흐름에서 독이 된다** — 15초짜리 나레이션 중간에
버튼이 튀어나온다.

같이 고친 것 셋:

- 장면이 끝난 뒤 마이크를 눌러도 아무 일이 없었다 (서버가 400 으로 막는데 화면엔
  표시가 없다) → `sceneEnded` 면 누름을 무시한다
- 등장인물 말풍선이 읽어주는 동안 숨겨져 있었다 → 시안(161:1158)대로 계속 보인다
- **다시 듣기를 읽는 도중에 누르면 갇혔다** — 원래 재생이 `stop()` 으로 끊겨 완료
  콜백이 안 와서 버튼이 영영 안 나타났다 → 다시 듣기도 끝나면 같은 신호를 준다

### ⚠️ 이 태블릿에는 한국어 TTS 음성이 0개다

`Speech.getAvailableVoicesAsync()` 로 확인했다 — **전체 6개 중 한국어 0개**,
기본 엔진이 `com.samsung.SMT:en_US` 다. 그래서 읽어주기가 즉시 실패하고
**"읽는 동안" 구간이 사실상 0초**다. 위 흐름을 실제로 보려면 기기 설정에서
한국어 음성을 설치해야 한다 (설정 → 접근성 → 음성 → TTS).

아이가 글을 못 읽는다는 게 이 앱의 전제라, **읽어주기가 안 되면 제품이 성립하지
않는다.** 시연 기기 점검 목록에 넣어야 한다.

## 3-6. 대화 장면에서 아이가 말하면 받아쓴 글이 뜬다 (임시)

**아이가 말할 수 있는 화면이 하나도 열리지 않던 것**을 풀었다. 원인은 [1-1](#1-1-️-대화가-통째로-열리지-않는다--모든-장면이-kind-narration) —
서버가 등장인물이 말을 거는 장면까지 전부 `narration` 으로 준다.

**마이크를 띄우는 기준을 `kind` 에서 `character_name` 으로 바꿨다.** 시안(161:1158)도
등장인물이 말을 걸면 마이크가 있는 화면이다. 줄거리만 흐르는 장면에는 대답할 상대가
없어 보내기만 나온다. 서버가 `kind: dialogue` 를 주기 시작해도 이 조건은 그대로 참이라
고칠 것이 없다.

**말을 글로 옮기는 건 당분간 앱이 한다.** 서버 STT 경로(`speak` → `child_text`)는
그대로 두고, 서버가 안 받아주는 장면에서만 안드로이드 내장 인식으로 받아쓴다.

| | 서버가 받아주는 장면 | 지금 (임시) |
| --- | --- | --- |
| 음성 | 녹음해서 `speak` 로 업로드 | 앱에서 받아쓰기 |
| 아이 말풍선 | 서버 STT 결과 | 받아쓴 글 (말하는 대로 늘어난다) |
| 등장인물 대답 | 서버가 준다 | 없다 |
| 리포트·단어장 | 쌓인다 | **안 쌓인다** |

**갈림길은 `serverAcceptsSpeech` 한 줄이다** (`StoryPlayScreen`). 서버가 대화 씬을
주기 시작하면 임시 경로는 저절로 안 쓰인다 — 지울 것도 없다.

받아쓰기 훅은 다시 말하기와 같이 쓰게 되어 `useRetellDictation` → **`useDictation`**
으로 이름을 바꾸고 `reset()` 을 더했다(장면이 바뀔 때 앞 장면 말이 남지 않게).

**실기기 확인**: 등장인물 없는 장면은 마이크 없음 / 있는 장면은 "말할 준비 완료" →
누르면 "듣고 있어요" + 말한 내용이 아이 말풍선에 실시간으로 들어감 → 다시 누르면 복귀.

## 3-7. 이야기를 끝내도 리포트가 "없다"고 뜨던 것

마이페이지 → 학습 리포트 보기 로 들어가면 **항상** "아직 리포트가 없어요" 가 떴다.

**리포트가 없어서가 아니라 어느 이야기인지 정해지지 않아서였다.** 리포트는
`GET /reports/{story_id}` 로 이야기 한 편당 하나인데, 마이페이지에서 들어올 때는
`story` 파라미터가 없어 `storyId` 가 빈 문자열이 되고, 그러면 조회가 아예 안 돌아
"에러도 아니고 데이터도 없는" 상태의 안내로 떨어진다. 라우트 주석에는 "그때는
진행 중인 이야기를 보여준다"고 적혀 있었는데 **구현이 안 돼 있었다.**

서버에 **리포트 목록 API 가 없어서** "가장 최근에 끝낸 이야기"를 물어볼 데가 없다
(`/progress/active` 는 진행 중인 것만 주는데, 리포트를 볼 시점엔 이미 끝나 null 이다).
그래서 **앱이 마지막으로 연 이야기를 기억한다** (`story/model/lastStoryStore`, id 만).
**리포트 목록 API 가 생기면 그 스토어는 지운다.**

그래도 못 정하면 문구를 **"어느 이야기의 리포트를 볼지 정해주세요"** 로 바꿨다 —
없는 것과 못 고른 것은 보호자가 할 일이 다르다.

## 3-8. 이야기 카드 사진이 타일을 안 채우던 것

서버 장면 사진을 **번들 아이콘 실측 크기(66/44)로 줄여 가운데** 두고 있었다. 선화
아이콘엔 맞는 크기지만 사진은 무엇을 그린 건지 안 보인다. 서버 그림만 타일을 꽉
채우게(`cover`) 하고 번들 아이콘은 그대로 뒀다.

## 3-9. 다시 말하기에서 못 빠져나오던 것

완료 버튼이 **받아쓴 글이 있을 때만** 열려서, 조용한 방이나 발음이 잘 안 잡히는
아이는 마이크를 눌렀다 멈춰도 계속 잠긴 상태였다. `no-speech` 는 "쉬는 중"으로 보고
재시작하므로 실패로도 안 잡힌다 — 실기기에서 실제로 갇혔다.

**말을 한 번 해보고 멈췄으면 연다.** 빈 텍스트가 가더라도 이야기를 끝낸 사실은
남는 게 낫다.

---

# 4. 08-16 에 고친 것 — 회귀 방지

**같은 문제로 다시 헤매지 않도록 원인까지 남긴다.**

## 4-1. `RECORD_AUDIO` 권한이 앱에서 통째로 빠져 있었다 ⚠️

**받아쓰기뿐 아니라 대화 화면의 아이 목소리 녹음(`expo-audio` → `speak`)도 동작할 수
없는 상태였다.** 서버가 죽어 있어 그 화면까지 못 가봐서 여태 안 드러났다.

원인이 둘 겹쳐 있었다.

1. `android/` 가 config plugin 추가 이전에 생성된 채였다. **`expo run:android` 는
   폴더가 있으면 prebuild 를 다시 돌리지 않는다** → 매니페스트가 계속 낡은 상태
2. prebuild 를 돌려도 `app.json` 의 이 줄이 마이크를 도로 뺐다

   ```json
   ["expo-image-picker", { "microphonePermission": false }]
   ```

   이건 "이미지 피커는 마이크가 필요 없다"가 아니라 **"앱에서 마이크 권한을 제거하라"**
   는 뜻이다 (매니페스트에 `tools:node="remove"` 가 붙는다).

**고친 것**: 그 줄을 지우고 `npx expo prebuild --platform android` 재실행.
카메라는 실제로 안 쓰므로(`launchImageLibraryAsync` 만 사용) `cameraPermission: false` 는 유지.

**확인법**

```bash
adb shell dumpsys package com.goodquestion.app | grep -A15 "requested permissions"
# android.permission.RECORD_AUDIO 가 있어야 한다
```

**교훈: `app.json` 의 plugin 설정을 바꿨으면 `expo prebuild` 를 직접 돌려야 한다.**
`expo run:android` 만으로는 반영되지 않는다.

## 4-2. 다시 말하기 받아쓰기(STT)를 앱에서 구현했다

서버 `retell` 이 **텍스트**(`retelling_text`)를 받는데 음성→글 변환 수단이 없어
`expo-speech-recognition`(안드로이드 내장 인식)으로 앱에서 직접 받아쓴다
(`features/story/hooks/useRetellDictation.ts`).

**서버가 `retell` 에서 음성 파일을 받게 바뀌면 이 훅을 버리고 `useChildRecorder` 를
쓰면 된다** — 갈아끼우는 자리는 `StoryRetellScreen` 한 곳이다.

### 이 기기(SM-T510 / 안드로이드 9)의 제약

- **`continuous` 옵션은 안드로이드 13+ 전용.** 못 쓴다 → 인식기가 문장마다 스스로 꺼진다
  → **끊길 때마다 다시 켜는 것을 앱이 직접 한다**
- **녹음 파일을 나중에 변환하는 기능도 안드로이드 13+ 전용.** 실시간 받아쓰기만 가능
- 오프라인 인식 없음 → **네트워크 필요**

### 실기기 로그로 잡은 결함 4건

| 결함 | 증상 | 원인 / 해결 |
| --- | --- | --- |
| 재시작 겹침 | 긴 문장이 `"아아"` 두 글자로만 잡힘 | 1초에 3번 켜져 마이크가 계속 닫혔다. `starting` 플래그 + 재시작 간격 300ms |
| 무음 판정 5초 | 아이가 뜸 들이면 끊김 | `EXTRA_SPEECH_INPUT_*` 로 침묵 3초 / 최소 청취 10초. **실측 10.4초로 적용 확인** |
| 두 번 누르면 초기화 | 받아쓴 글이 사라짐 | 이미 듣는 중이면 `start()` 무시 |
| **빈 최종 결과에 문장 소실** | 앞 문장이 다음 문장에 덮여 사라짐 | 안드로이드가 `onResults(), results: []` 를 주는 경우가 있다. 구간이 끝날 때 미확정 문장을 `finals` 로 건져낸다(`flushInterim`) |

**확인 완료**: `아기 돼지 삼형제가 살았어요 늑대가 문을 두드렸어요` — 쉬는 구간을 넘어
두 문장이 다 남았다.

## 4-3. 로그인이 프로필 동기화에 묶여 막혀 있었다

> **이 절은 낮의 판단이다. 저녁에 뒤집혔다 — 3-11 을 같이 본다.**

예전 구조는 로그인 직후 `POST /auth/sync-profile` 을 부르고, **실패하면 Supabase 세션까지
되돌렸다.** 그래서 백엔드 설정 문제 하나로 **아무도 로그인할 수 없었다.**

**고친 것**: `useEmailLogin` 이 Supabase 호출 하나만 한다. 프로필 행 만들기는
회원가입 경로(`useSignUp`)에만 남겼다.

> 사용자 지시로 뺐다. 콘솔에서 직접 만든 계정처럼 앱 가입 흐름을 안 탄 계정은
> 서버 프로필이 없을 수 있다 — 나중에 "프로필이 등록되지 않은 사용자입니다" 를 받으면
> 그때 한 번 부르고 재시도하는 방식(lazy repair)으로 보완하는 게 맞다.

## 4-4. 인증 코드 점검에서 나온 것 3건

| # | 문제 | 해결 |
| --- | --- | --- |
| ① | **백그라운드 복귀 시 토큰 갱신이 안 됨** | `autoRefreshToken: true` 만으로는 RN 에서 부족하다. 백그라운드에서 타이머가 멎고 돌아와도 안 살아난다. `AppState` 에 `startAutoRefresh`/`stopAutoRefresh` 연결 (`supabase.ts`) |
| ③ | **콜드 스타트에서 토큰 없이 요청이 나감** | 요청 인터셉터가 토큰이 비면 저장 세션에서 꺼내온다. `client.ts` 가 supabase 를 직접 import 하면 순환 참조라 **토큰 로더를 등록받는 방식**으로 뒤집었다. 동시 요청은 한 번만 읽고 3초 상한 |
| ④ | **3초 타임아웃에 걸리면 로그인 화면에 갇힘** | `useAuthSession` 에 `isResolved` 추가. `isLoading`(화면 그려도 되나)과 `isResolved`(세션을 실제로 읽었나)를 분리. `AuthGate` 의 "앱 켠 직후 한 번만" 판정을 `isResolved` 기준으로 |

**①은 태블릿 앱이라 특히 중요하다.** 아이가 잠깐 두고 돌아오는 게 기본 사용 패턴인데,
그때마다 이야기 도중에 401 로 끊긴다.

**③의 효과는 로그로 확인했다.**

```
전: GET /stories → 401 {"detail":"Not authenticated"}        (헤더 없음)
후: GET /stories → 401 {"detail":"유효하지 않은 토큰입니다."}   (헤더 실림)
```

그리고 `ensureServerProfile` 의 에러 복구 `signOut()` 에 **`{ scope: 'local' }`** 을 넣었다.
인자를 안 주면 supabase-js 기본이 `global` 이라 **그 계정의 모든 기기 세션**이 무효화된다.
(마이페이지의 사용자 로그아웃과 회원탈퇴는 global 유지)

## 4-5. 디자인 대조에서 나온 어긋남 5건

**디자인은 08-15 이후 바뀌지 않았다.** 노드 스냅샷 대조 결과 추가·삭제·크기변경 0건.
어긋난 건 전부 구현 쪽이었다.

| # | 화면 | 내용 |
| --- | --- | --- |
| 1 | 대화 3화면 | **씬 단어 고르기를 시안대로 다시 만들었다.** 그림 위 오른쪽(19·11)에 겹치는 **접힘 목록**(108폭, 체크박스+화살표)인데 인라인 알약 칩으로 돼 있었다 |
| 2 | 다시 말하기 | **핵심 단어**가 카드 제목이었다. 서버가 `submit` 응답으로 주는 진짜 낱말을 받아놓고 안 넘기고 있었다 → 라우트 파라미터로 전달 |
| 3 | 비밀번호 찾기 1단계 | 제목이 **"비밀번호를 잊으셨나요?"** 인데 없었다 |
| 4 | 비밀번호 찾기 3단계 | 입력칸이 **"새 비밀번호 / 새 비밀번호 확인"** 인데 회원가입용을 그대로 썼다 |
| 5 | 로그아웃 모달 | 두 줄 다 창작 문구였다 → **"로그아웃 하시겠어요?" / "다시 로그인해야 서비스를 이용할 수 있어요"** |

**1번은 지난 세션의 오독이었다.** "화면 프레임 밖 스케치라 구현 안 함"으로 넘겼는데,
조상 경로를 뽑아보니 `417:602 / 417:609 / 417:569` 가 대화 화면 세 개 **안에** 있었다.
**노드가 어디 속하는지는 이름이 아니라 부모 경로로 확인한다.**

### 일치 확인된 화면 (손댈 것 없음)

로그인 · 회원가입 4단계 · 아이 등록 · 메인 화면 · 이야기 목록 · 단어장 · 마이페이지 ·
보호자 확인 모달 · 회원탈퇴 모달 · 순서 맞추기 4상태 · 완료 화면 · 리포트 4프레임

## 4-6. 아이 선택 화면을 모달로 바꿨다 (사용자 결정)

피그마에 **두 벌**이 있다. 이름만 보고 고르면 안 된다.

| | `86:734` **모달** (지금 이것) | `92:889` 전체 화면 |
| --- | --- | --- |
| 제목 | 아이 프로필 선택 | 누구의 이야기를 만나볼까요? |
| 부제 | 누구의 이야기를 이어갈까요? | 아이를 선택해주세요 |
| 아바타 | **이름 첫 글자 뱃지** 72 | 동물 그림 |
| 버튼 | **변경하기** | 시작하기 |
| 배경 | 마이페이지 | 없음 |

**사용자 결정: 모달(A)을 쓰되 배경은 없앤다.** 로그인 직후엔 뒤에 보일 화면이 없다.
카드 480×412, 안쪽 여백 24(Modal 기본 28과 다름), 버튼 432×52 — 전부 실측값.

- **닫기 X 는 `onClose` 를 넘길 때만 나온다.** 아이를 고르기 전엔 갈 데가 없어서,
  닫히면 빈 화면에 갇힌다. 마이페이지에서 쓸 땐 `onClose` 를 주면 된다
- 닫기 아이콘은 Figma(`86:739`)에서 내보내 `assets/icons/close.svg` 로 추가.
  획 두 개로 나뉘어 있어 한 파일로 합쳤다(path 값은 원본 그대로)
- `ChildProfileModal` 을 `features/account` → **`shared/ui`** 로 옮겼다.
  `features/child` 화면이 쓰려면 feature 간 import 가 되기 때문. 같은 성격의
  `ChildSwitcher` 가 이미 거기 있다

### 오류·빈 상태에서 나온 결함 — 다른 화면에도 같은 종류가 있을 수 있다

아이 선택 화면의 오류 상태가 이렇게 떴다.

```
아이를 선택해주세요            ← "고르라"면서
아이 목록을 불러오지 못했어요   ← "못 불러왔다"고 하고
[ 시작하기 ]                  ← 눌리지도 않고 나갈 방법도 없음
```

**본 화면 위에 오류 문구만 얹으면 이렇게 된다.** 안내와 할 일만 남기고 "다시 시도"를
붙였다(`useActiveChild` 에 `retry` 추가).

같이 나온 것 둘 — **다른 화면에서도 같은 실수가 나기 쉽다.**

- `EmptyState` 는 기본이 `flex: 1` + 세로 여백 `5xl` 이라, 그대로 쓰면 **자리를 늘려
  주변을 밀어낸다.** 버튼과 한 덩어리로 모으려면 둘 다 꺼야 한다
- `Button` 은 기본이 `alignSelf: 'flex-start'` 라 **부모의 `alignItems: 'center'` 를
  덮어쓴다.** 가운데 두려면 `alignSelf: 'center'` 를 직접 준다

---

## 4-7. 이야기 목록이 통째로 비던 것 — 응답을 감싸서 준다 ⚠️

**서버가 명세와 다른 모양으로 준다.**

```json
{"items":[{"id":"...","title":"방귀 뀌는 며느리","thumbnail_url":null,
           "estimated_minutes":20,"topics":["다름","자기이해","장점 발견"],
           "difficulty":"보통"}],
 "total":1,"limit":20,"offset":0}
```

명세는 **배열**인데 실제로는 `{ items, total, limit, offset }` 로 감싸서 온다.
앱은 배열이라 보고 `.map()` 을 돌려서 **목록이 통째로 비었다.** 화면에는 에러가 아니라
"아직 이야기가 없어요"가 떠서 서버 문제로 보이기 쉽다.

`toStoryList()` 로 **둘 다 받게** 했다 (단어장의 `parseWordList` 와 같은 방식).
`/main` 의 `recommended_stories` 에도 같은 가드를 넣었다.

> **다른 목록 엔드포인트도 같은 함정이 있을 수 있다.** 화면이 비면 파싱 실패를
> 먼저 의심하고, `[API]` 로그에 에러가 없는지부터 본다 — 에러가 없는데 비어 있으면
> 파싱이다.

응답에 명세에 없는 `difficulty: "보통"` 도 들어 있다 (지금은 안 쓴다).

## 4-8. 프로필 사진 업로드 403 — RN 이 Content-Type 을 덮어쓴다 ⚠️

사진을 고르면 아이 등록이 실패했다. 단계별로 찍어보니 **주소 발급(우리 서버)까지는
정상**이고 **S3 PUT 에서 403** 이었다. S3 응답 XML 이 원인을 그대로 알려줬다.

```
<Code>SignatureDoesNotMatch</Code>
CanonicalRequest:
  X-Amz-SignedHeaders=content-type;host
  content-type:                          ← 비어 있음
```

presigned URL 은 **`content-type` 을 서명에 포함**해서 만들어지는데(백엔드가
`image/jpeg` 로 서명), 우리 PUT 은 **빈 Content-Type** 으로 나가고 있었다.

`headers` 에 분명히 넣었는데도 그런 이유는 —
**React Native 는 Blob 을 body 로 보낼 때 그 Blob 의 `type` 으로 `Content-Type` 을
덮어쓴다.** `fetch(uri).blob()` 로 읽은 파일은 `type` 이 비어 있다.

```ts
const raw = await (await fetch(uri)).blob();
// 이 한 줄이 없으면 S3 가 403 을 준다.
const body = raw.type === contentType ? raw : new Blob([raw], { type: contentType });
```

**교훈: RN 에서 presigned PUT 을 할 때는 Blob 의 `type` 을 반드시 맞춘다.** 헤더만
넣어서는 안 된다.

> 확인할 때 사진첩에 개인 문서 사진이 많았다. **그런 건 서버에 올리지 않는다** —
> 시험용 이미지를 `adb push` 로 넣어 쓰고 끝나면 지웠다.

## 4-9. 사진을 올린 아이는 이름 글자 대신 사진을 보여준다

시안(86:734)은 첫 글자 뱃지만 그려져 있는데, **사진을 올린 아이까지 글자로 덮으면
"내가 올린 사진이 어디 갔지"가 된다.** 사진이 있으면 사진이 우선이다.

세 화면이 같은 `InitialBadge` 를 쓰고 있어서 **컴포넌트 자체가 `photoUrl` 을 받게** 했다.
화면마다 이미지 코드를 복붙하면 나중에 한 곳만 고치고 넘어간다.

적용된 자리: 아이 선택 모달 · 마이페이지 아이 카드 · 홈/리포트 전환 알약 · 전환 드롭다운.
(대화 화면의 등장인물 뱃지 `SpeechBubble` 은 아이가 아니라 이야기 속 인물이라 제외)

**아직 화면에서는 확인 못 했다** — 서버가 객체 키를 주기 때문 (1-2 참고).

## 4-10. 마이페이지 연필이 수정이 아니라 **새 아이를 만들던** 버그 ⚠️

```
연필 → router.push('/child/create')   ← id 를 안 넘겨서 그냥 "등록" 화면
     → 저장하면 POST /users/me/children → 새 아이 생성
```

`PATCH /users/me/children/{id}` 를 쓰는 `useUpdateChild` 는 **만들어만 놓고 화면에 안
붙어 있었다.**

고친 것: 연필이 `?id=` 를 넘기고, 등록 화면이 그걸 받으면 **수정 모드**가 된다.

| | 등록 | 수정 (`?id=`) |
| --- | --- | --- |
| 제목 | 아이 프로필을 만들어주세요 | **아이 프로필을 수정해주세요** |
| 초기값 | 빈칸 + 기본 아바타 | **그 아이의 이름·아바타·사진** |
| 버튼 | 등록 완료 | **수정 완료** |
| 호출 | `POST` | **`PATCH`** |
| 끝나면 | 활성 아이 지정 → `/child/select` | **`router.back()`** |

**수정 화면 시안은 아직 없다.** 만들기 시안을 그대로 쓰고 제목·버튼 문구만 바꿨다 —
시안이 나오면 교체 대상이다.

### 이름만 고쳐도 사진이 사라지던 것

고치는 김에 발견했다. 예전 수정 로직은 `avatarId` 를 **항상** 보냈다. 그러면 사진을
올려둔 아이의 **이름만 고쳐도** `avatar:bear` 표식이 `profile_image_url` 을 덮어써서
**사진이 사라진다.**

| 사용자가 한 일 | 서버로 보내는 것 |
| --- | --- |
| 이름만 고침 | `name` 만 — **그림은 손대지 않음** |
| 아바타를 골랐다 | `name` + `avatarId` |
| 사진을 새로 골랐다 | `name` + `photo` |

`PATCH` 는 보낸 항목만 바꾸므로 **빼는 것이 곧 유지다.**

수정 화면에서 기존 사진이 "사진 바꾸기" 칸에 **이미 선택된 상태로** 보인다. 빈 카메라
칸만 보이면 사진이 날아간 줄 알고 다시 올리게 된다.

**사진 선택기를 열었다가 취소한 경우는 "바꾼 것"이 아니다** — "열었는지"가 아니라
"골랐는지"(`photo.picked`)로 판단한다.

`updateChild` 에도 사진 업로드를 붙였다. 예전에는 `name` 과 `avatarId` 만 받아서
수정 화면에서 사진을 골라도 **조용히 버려졌다.**

## 4-11. 로그인에서 `sync-profile` 을 되살렸다 (실패해도 안 막게)

08-16 낮에 로그인이 프로필 동기화에 묶여 막히길래 호출을 **뺐다**(4-3). 그런데 백엔드
확인 결과 **서버 `parents` 행이 없으면 나머지 API 가 전부 401** 이고, `sync-profile`
한 번이면 그 행이 만들어진다. 그래서 되살렸다 — **다만 실패가 로그인을 막지 않는다.**

| 호출처 | 동작 |
| --- | --- |
| 회원가입 3단계 | `ensureServerProfile` — **실패하면 던진다** (행이 만들어져야 하는 자리) |
| 로그인 | `tryEnsureServerProfile` — **실패해도 통과** (보정용) |
| 비밀번호 재설정 | 안 부름 (기존 계정) |

`ensureServerProfile` 에서 **`signOut()` 을 완전히 뺐다.** 프로필 행 만들기는 부수적인
일이지 인증의 전제가 아니다.

**로그인에서 `await` 로 기다린다** — 안 기다리면 아이 목록 요청이 프로필 생성보다 먼저
나가서 또 401 을 받는다.

---

# 5. 진단하는 법

## 5-1. API 실패 로그

앱이 API 실패를 로그로 안 남겨서 원인을 못 봤다. `apiClient` 응답 인터셉터에 임시 로그를
넣었다(6장). **서버 본문까지 찍는 게 핵심이다** — 401 이 "토큰이 틀렸다"인지 "헤더가
없다"인지는 본문으로만 갈린다.

```
[API] GET /users/me → 401 unauthorized :: {"detail":"유효하지 않은 토큰입니다."}
```

## 5-2. 콘솔 (사용자가 직접 보는 것)

```bash
adb logcat -T 1 -s ReactNativeJS:V ExpoSpeechService:V
```

`[API]` 로그는 **axios 인터셉터**라 우리 백엔드 요청만 찍힌다. **Supabase 요청은
`supabase-js` 가 자체 fetch 로 보내서 안 찍힌다.** 그래서 Supabase 대시보드 로그와
RN 콘솔이 서로를 못 본다. (필요하면 `createClient` 에 `fetch` 를 감싸 넘기면 된다)

## 5-3. 디자인이 바뀌었는지

`get_metadata(0:1)` 응답이 파일로 떨어진다. 이전 세션의 응답 파일과 파싱 비교하면
추가/삭제/크기변경이 그대로 나온다. 이전 파일 위치:

```
~/.claude/projects/<프로젝트>/<세션id>/tool-results/*get_metadata*.txt
```

노드가 **어느 화면에 속하는지**는 조상 경로를 뽑아 확인한다. 이름만 보면 틀린다(4-5).

## 5-4. 명세(Notion) — 착시 하나 주의

**데이터베이스 id 가 조회할 때마다 바뀐다.** 새 행들의 `createdTime` 이 조회 시각과
초 단위로 같은 걸 보면 안다. Notion MCP 가 인라인 데이터베이스를 조회할 때마다 새
스냅샷을 만드는 것이지 **명세가 다시 쓰인 게 아니다.**
(예전 인수인계에 "08-15 저녁에 통째로 다시 만들어졌다"고 적혀 있던 것도 같은 착시였다.)

**겉만 보지 말고 상세 페이지 본문까지 읽는다.** 답은 응답 예시와 그 밑 설명 문장에 있다.

**`/api` prefix 를 붙이지 않는다.** 상세 페이지 EndPoint 는 `/api/...` 로 적혀 있지만
배포 서버는 그 경로를 모른다.

---

# 6. 지금 들어 있는 임시 코드 (연동 확인 끝나면 지운다)

| 위치 | 내용 |
| --- | --- |
| `src/shared/api/client.ts` | `[API]` 실패 로그 (응답 인터셉터) |

**전체 흐름을 확인할 때까지는 두는 게 유리하다** — 어느 요청이 왜 막히는지 바로 보인다.
오늘 이 로그 하나로 원인을 세 번 잡았다(JWT · 목록 파싱 · S3 서명).
끝나면 지우거나 `__DEV__` 로 감싼다.

> `_layout.tsx` 의 시작 시 프로브와 업로드 단계별 로그는 **이미 제거했다.**
> 프로브는 `child_id=probe` 를 보내서 422 노이즈를 만들고 있었다.

---

# 7. 인증 구조 (참고)

```
① 로그인   앱 → Supabase     : 이메일/비번 → JWT 발급   ★ 이게 로그인이다
② 그 외    앱 → 우리 백엔드   : Authorization: Bearer <①의 JWT>
```

우리 백엔드는 **비밀번호를 모른다.** 토큰의 서명만 검증해서 "진짜 Supabase 가 발급했나,
누구 것인가"를 판단한다. 그래서 로그인 엔드포인트가 명세에 없다.

토큰은 화면에서 다루지 않는다 — `startAuthTokenSync()` 가 `onAuthStateChange` 를 구독해
`apiClient` 에 자동으로 싣는다. 로그인 직후에는 리스너를 기다리지 않고 세션에서 직접
싣는다(콜백이 늦게 도는 경우가 있다).

## 회원가입은 `signUp()` 이 아니라 OTP 흐름이다

```
1단계  signInWithOtp({ shouldCreateUser: true })  → 메일로 코드, 계정 생성
2단계  verifyOtp({ type: 'email' })               → 코드 확인. 여기서 세션이 생긴다
3단계  updateUser({ password }) + sync-profile    → 비밀번호 설정, 서버 프로필 생성
4단계  "아이 등록하기" → /child/create → 등록 완료 → /child/select
```

시안이 비밀번호를 **코드 확인 뒤에** 받는데 `signUp` 은 그보다 먼저 필요하고,
Confirm email 이 켜져 있으면 `signUp` 직후 세션이 없어 `sync-profile` 을 부를 수 없다.

**비밀번호 찾기도 같은 흐름이되 `shouldCreateUser: false` 다.** true 면 오타 친 주소로
새 계정이 조용히 생긴다.

**⚠️ 같은 `otp_disabled` 코드가 흐름에 따라 뜻이 다르다.** 가입에서는 "OTP 가입이 꺼져
있다"지만 비밀번호 찾기에서는 **"그 이메일로 가입된 계정이 없다"** 다.

**인증코드는 8자리 / 10분** (서버 설정). 시안은 6칸이지만 **8칸이 맞다(사용자 확인).**

## Supabase 프로젝트는 팀원 소유다

`.env` 의 `vytcszxecrvdbwaixrhi` 는 우리 계정 것이 아니다. **콘솔을 만져야 하는 일은
전부 팀원에게 부탁해야 한다.** 메일 발송은 해결됐다(커스텀 SMTP + `{{ .Token }}`).

**앱에 절대 넣지 않는 값**: `SUPABASE_JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`.

---

# 8. 화면별 연동 메모

## 대화 화면 — 장면을 서버가 굴린다

```
POST /progress/{story_id}/start              → 세션 + 첫 장면
  나레이션: POST .../steps/{n}/complete       → 다음 장면
  대화:     POST .../steps/{n}/speak          → scene_ended 가 true 가 될 때까지
POST /progress/{story_id}/steps/{n+1}        → 다음 장면 진입
```

- **미션은 `kind` 가 아니다.** `mission !== null` 로 판단한다
  - ⚠️ 다만 "씬 진행" 상세 페이지 맨 밑에는 `kind: narration | dialogue | mission` 이라고
    적혀 있다. **명세 안에서 모순된다.** 백엔드 확인 대상
- **`speak` 만 `multipart/form-data`** 다
- **아이당 `in_progress` 세션은 하나뿐.** 다른 이야기가 진행 중이면 409 — 실패가 아니라
  "읽던 이야기를 마저 보라"고 안내해야 한다
- 씬 단어 고르기는 그림 오른쪽 위 접힘 목록이다 (4-5 참고)

## TTS 는 앱이 구현한다

서버는 글만 준다. `expo-speech` → `useNarrationSpeech`. 다 읽으면 아이 차례로 넘어가고,
읽어주기가 안 되는 기기를 위해 2.5초 타이머를 안전장치로 함께 건다.

## 활동 화면

- **정오답은 서버가 판정한다.** 앱은 정답 순서를 모른다
- **카드는 매 요청마다 섞여 온다.** `staleTime: Infinity`, 오답일 때 무효화하지 않는다
- **완료 화면 수치는 `retell` 응답이 준다.** 라우트 파라미터로 넘긴다
- **핵심 단어는 `submit` 응답이 준다.** 다시 말하기 화면으로 라우트 파라미터로 넘긴다

## 리포트 화면

- **자동 생성되지 않는다.** `generate` 를 부르고 **폴링**한다(`generating` 이면 3초마다).
  409 는 "이미 만드는 중"이라 실패가 아니다
- 서버 응답 → 화면 타입 변환은 `features/report/model/fromApi.ts` 한 곳
- `strength`/`tip` 은 둘 중 하나만 올 수 있고 `quotes` 는 빌 수 있다

## 단어장

**전체 조회 응답이 명세 안에서 두 가지로 적혀 있다.** 배열과 `{ total, items }`.
`parseWordList` 가 둘 다 받는다.

⚠️ **쿼리 파라미터도 두 갈래다** — 단어장 페이지는 `story_id`, 보고서 페이지 부록은
`kind`/`limit`/`offset`. 앱은 둘 다 보내는데, 서버가 하나만 알면 **나머지는 조용히
무시된다**(에러가 아니다). 이야기별 필터가 안 먹으면 이걸 의심한다.

하트는 낙관적 갱신이고 **저장 시 409 는 성공으로 친다.**

---

# 9. 명세에 없어서 앱에서 정한 것

| 애매했던 것 | 정한 것 | 고칠 자리 |
| --- | --- | --- |
| 자녀에 아바타 필드가 없다 | `profile_image_url` 에 `avatar:bear` 표식 | `child/model/avatarRef.ts` |
| `object_key` 를 그대로 써도 되나 | **`http(s)` 로 시작할 때만 사진으로 인정** | 손댈 것 없음 |
| 업로드 `target` 값 목록이 없다 | `'child'` 전송 | `child/api/profileImageApi.ts` |
| 생년을 못 보내는데 age 가 온다 | 0이면 빈 칸 | 손댈 것 없음 |
| `sync-profile` 의 `name` 출처 | 소셜 메타데이터 → 이메일 @ 앞부분 | `auth/model/profileName.ts` |

**보호자 이름은 가입 단계에서 받지 않는다**(사용자 결정). 나중에 '계정 설정' 화면에서
`PATCH /users/me` 로 고치게 한다.

---

# 10. 실기기 태블릿 (SM-T510) — 함정 모음

**이 태블릿은 armeabi-v7a(32비트)다.** 에뮬레이터용 x86_64 APK 는 안 들어간다.

```bash
adb devices                                    # device 로 잡히는지 먼저
npx expo run:android --no-bundler              # 네이티브 모듈을 새로 넣었을 때만
npx expo start --dev-client --clear
adb reverse tcp:8081 tcp:8081
adb shell am force-stop com.goodquestion.app
adb shell am start -a android.intent.action.VIEW \
  -d "exp+good-question-app://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081"
```

- **`expo run:android` 가 gradle 을 부르기 전에 멈추는 일이 있다.** 오늘 25분간
  아무것도 안 하고 매달려 있었다. `android/app/build` 가 안 생기고 gradle 데몬 로그가
  health check 만 찍으면 그것이다. **`cd android && ./gradlew assembleDebug` 로 직접
  돌리면 통과한다.** 그 뒤
  `adb install -r android/app/build/outputs/apk/debug/app-debug.apk`
- **APK 가 268MB 라 설치에 몇 분 걸린다.** 타임아웃을 넉넉히 준다
- **Metro 를 네이티브 빌드와 동시에 띄우지 않는다.** Metro 파일 감시가 gradle 이 지우는
  CMake 임시 폴더를 따라가다 `ENOENT ... boo.dir` 로 죽는다. 빌드가 끝난 뒤 띄운다
- **태블릿을 뽑았다 꽂으면 `adb reverse` 가 풀린다.** 다시 걸어야 한다
- **딥링크는 앱이 전면일 때 라우팅되지 않는다.** `force-stop` 후 열거나 화면 안에서 이동
- `adb shell input text` 는 빠른 입력에서 글자를 흘린다. 한글은 아예 못 넣는다
- 화면 확인은 `adb exec-out screencap -p > a.png`
- **비밀번호는 대신 입력하지 않는다.** 로그인 화면까지만 띄우고 사용자에게 넘긴다

**네이티브 모듈이 넷 들어 있다** — `expo-image-picker`, `expo-audio`, `expo-speech`,
**`expo-speech-recognition`(오늘 추가)**. 이보다 오래된 APK 로는 해당 화면에서 터진다.

> `expo-speech-recognition` 최신이 SDK 56용(`56.0.1`)이라 SDK 57 빌드가 아직 없다.
> peerDeps 가 `*` 라 설치·동작은 확인했다. SDK 를 올릴 때 다시 본다.

---

# 11. 검증 방법

1. `npm run typecheck && npm run lint`
2. 실기기에 올려 실제로 눌러본다
3. 수치가 중요하면 `npm run web` + `getComputedStyle` 로 Figma 실측값과 대조
4. 긴 이름·많은 항목·**빈 목록·오류 상태**를 넣어 흔들어본다 ← 오늘 여기서 결함이 나왔다

**화면이 코드와 다르면 캐시부터 의심한다** (AGENTS.md). `--clear` 로 재시작.

**측정할 때 등장 애니메이션을 조심할 것.** `Appear` 의 `from="scale"` 이 끝나기 전에
재면 폭이 0.92배로 나온다. 최소 2초 기다린 뒤 잰다.

---

# 12. 아직 TODO

> 구현 안 된 것 목록은 [2-3](#2-3-아직-구현-안-된-것) 에도 있다. 여기는 그 외의 것들이다.

- 계정 설정 화면, 도움말 화면 (둘 다 디자인 준비 중)
- **소셜 로그인** — `LoginScreen` 의 `handleSocialPress` 가 TODO. 콘솔에 어떤 provider 가
  Enabled 인지, `goodquestion://oauth` 가 URL Configuration 에 등록됐는지부터 팀원 확인 필요.
  PKCE 인지 implicit 인지에 따라 복귀 처리가 다르다(`exchangeCodeForSession` vs `setSession`)
- **회원탈퇴는 붙였지만 한 번도 안 눌러봤다.** 되돌릴 수 없으니 테스트 계정을 하나 더 만든다
- (디자이너 확인 대기) 인증코드 6칸 vs 8칸, 이야기 상세에서 뺀 세 블록,
  리포트 프레임 폭 1007 → 1024, 이야기 줄의 사람 아이콘, 마이페이지의 집 모양 아이콘,
  "궁금해한 어휘" 칩의 갈색 글자, 회원탈퇴 모달의 `재 로그인` 띄어쓰기(시안 오타로 보고 안 따랐다)

---

# 13. 테스트 계정

```
abccba111307@gmail.com / goodq1234   (Supabase 에 실제로 존재한다)
```

이 계정에 아이가 넷 있다 — `아이` · `아이3` · `아이1` · **`photo`**.
`photo` 는 08-16 에 사진 업로드를 확인하려고 만든 것이다. **명세에 아이 삭제 API 가
없어서 앱에서는 못 지운다.** 백엔드에 부탁하거나 그대로 두면 된다.
