# 인수인계 (2026-08-15, 로그인·회원가입 연동 완료 — 다음은 소셜 로그인)

작업 규칙은 [AGENTS.md](AGENTS.md), 실행 방법은 [README.md](README.md) 에 있다.
이 문서는 **둘 중 어디에도 없는 것** — 지금까지의 판단과 다음에 할 일 — 만 담는다.

## 어디까지 됐나

Figma 섹션 `182:303`(퍼블리싱 해도 되는 페이지)에서 **그때까지 올라와 있던 화면은 전부 구현**했다.

로그인 / 회원가입(4단계) / 비밀번호 찾기(3단계) / 아이 프로필 생성·선택 /
메인 화면(홈) / 이야기 목록·상세 / 이야기 전개 및 대화(도입·대화·미션) /
이야기 후 활동(순서 맞추기·다시 말하기·완료) /
단어장(전체·이야기별) / 단어 상세 / 마이페이지 / 보호자 리포트 /
아이 전환 드롭다운 / 모달 3개(보호자 확인·로그아웃·회원탈퇴)

전부 안드로이드 태블릿 에뮬레이터(1280×800 dp)에서 눌러가며 확인했고, 주요 화면은
브라우저에서 computed style 을 뽑아 Figma 실측값과 수치 대조까지 했다.

## 뒤늦게 올라온 디자인 — 전부 구현했다

아래는 2026-08-09 에 올라온 것들이고, 마지막 줄만 2026-08-11 에 퍼블리싱 쪽으로
옮겨져 그때 구현했다.

| 화면                 | node                                    | 어디에 있나                                   |
| -------------------- | --------------------------------------- | --------------------------------------------- |
| 메인 화면(홈)        | `78:184`                                | `story/screens/HomeScreen`                    |
| 이야기 도입          | `86:410`                                | `StoryPlayScreen` 의 `blocked` 상태           |
| 이야기 전개 및 대화  | `161:1158`                              | `StoryPlayScreen` 의 `listening` 상태         |
| 대화(미션 있을 시)   | `216:277`                               | `StoryPlayScreen` 의 미션 패널                |
| 마이크 상태 가이드   | `86:448`                                | 화면이 아니라 스펙. `MicControl` 4상태의 근거 |
| 활동 1/2 순서 맞추기 | `92:993` `92:1094` `92:1157` `92:1215`  | `StoryActivityScreen` (초기·진행·오답·정답)   |
| 활동 2/2 다시 말하기 | `92:1290`                               | `StoryRetellScreen`                           |
| 활동 완료            | `92:1340`                               | `StoryDoneScreen`                             |
| 보호자 리포트        | `252:193` `252:249` `252:297` `287:273` | `report/screens/ReportScreen` (2026-08-11)    |

이야기 하나가 이렇게 이어진다.

```
목록 → 상세 → /story/[id]/play → /activity → /retell → /done → 홈 또는 목록
```

리포트는 이 흐름과 따로 떨어져 있다 — 마이페이지에서 보호자 확인을 통과해 들어간다.

```
/mypage → 학습 리포트 보기 → (보호자 확인) → /report
```

### 파일에 섹션이 두 개다 (2026-08-11 갱신)

캔버스 최상위에 섹션이 둘 있다.

| 섹션      | 이름                             | 상태                                   |
| --------- | -------------------------------- | -------------------------------------- |
| `182:303` | 퍼블리싱 해도 되는 페이지        | **전부 구현 완료** (최대 노드 287:296) |
| `182:304` | 아직 퍼블리싱 하면 안되는 페이지 | 옛 버전 `118:210` 하나만 남았다        |

**2026-08-09 에 "구현하지 말라"고 적어둔 학습 리포트가 그 뒤 퍼블리싱 쪽으로
옮겨져서, 이번에 구현했다.** `252:193` `252:249` `252:297` 이 `182:303` 안의
새 섹션 `287:262`(보호자 리포트)로 이동했다. `182:304` 에는 예전 주간/월간 요약
시안(`118:210`)만 남아 있고, 이건 지금 화면과 구성이 달라 손대지 않았다.

이때 같이 생긴 것:

| 노드      | 이름        | 처리                                          |
| --------- | ----------- | --------------------------------------------- |
| `287:273` | 비활성화 시 | 이전/다음 리포트 버튼의 비활성 상태. 반영했다 |
| `287:296` | 메인페이지  | 기존 `78:184` 을 감싼 섹션일 뿐. 새 내용 없다 |

**다음에 확인할 것**: 두 섹션에 새 프레임이 생겼는지, 그리고 `182:304` 의
`118:210` 이 다듬어져 넘어오는지를 본다. 노드 ID 가 클수록 최근에 만든 것이다.

### 아이 전환은 모달이 아니라 드롭다운이다 (2026-08-12)

같은 동작에 시안이 두 개 있다. 섹션 전체를 훑다가 어디에도 매핑되지 않는 노드로
발견했다.

| 노드                | 무엇                                | 상태                         |
| ------------------- | ----------------------------------- | ---------------------------- |
| `181:889` `181:920` | 알약 아래 펼쳐지는 드롭다운         | **이걸 구현했다**            |
| `86:734`            | 화면 가운데 "아이 프로필 선택" 모달 | 구현돼 있지만 지금은 안 쓴다 |

드롭다운이 노드 번호가 더 나중이고, 보호자 리포트·메인페이지 섹션에 각각 하나씩
알약 자리에 놓여 있어 현재 의도로 봤다(사용자 확인을 받았다).

`ChildSwitcher` 가 목록을 직접 들고 펼친다 — 알약의 화면 좌표를 `measureInWindow`
로 재서 그 자리에 겹쳐 그리는 방식이다. 첫 줄이 알약을 덮고 화살표만 뒤집힌다.
줄 높이는 디자인 43 대신 48로 올렸다(줄이 붙어 있어 hitSlop 으로 못 넓힌다).

**`ChildProfileModal` 은 남겨뒀지만 지금 아무 데서도 쓰지 않는다.** `86:734` 시안이
살아 있어서 지운 게 아니다. 모달로 되돌리려면 라우트에서 다시 붙이면 된다.

**드롭다운에는 "아이 추가하기"가 없다** — 디자인에 없다. 추가는 마이페이지에서 한다.

### 리포트에도 이야기 선택 드롭다운이 생겼다 (2026-08-12)

이야기 줄(`252:202`)에 화살표가 붙어(`320:194`) 트리거가 됐고, 펼치면 `320:197`
(244×168, 56짜리 줄)이 나온다. 다른 이야기의 리포트로 바로 넘어간다
(`features/report/components/StoryPicker`). "이전/다음 리포트 보기" 버튼은 그대로 둔다.

**목록 순서는 저장 순서(오래된 것 → 최신) 그대로 두고 현재 것에 화살표를 달았다.**
시안은 첫 줄에만 화살표가 있는데 줄 내용이 전부 자리표시라 "첫 줄 = 현재"인지
"목록 전체 + 현재 표시"인지 구분되지 않는다. 아래 이전/다음 버튼과 순서를 맞추는
쪽을 골랐다. **디자이너 확인이 되면 바꾸면 된다.**

### 디자인이 바뀌었는지 확인하는 법

노드를 눈으로 훑지 말고 스냅샷을 비교한다. `get_metadata(0:1)` 응답이 파일로
떨어지므로, 이전 응답 파일과 지금 것을 파싱해 노드 id·이름·크기를 비교하면
추가/삭제/크기변경이 그대로 나온다. 2026-08-12 에 이 방법으로
**추가 91 · 삭제 72 · 크기변경 11** 을 찾았다 (전부 대화 화면과 리포트 드롭다운).

여기까지 훑은 방법:

```bash
# 최상위 구조만 (섹션 목록)
get_metadata(fileKey) → 페이지 목록 → get_metadata(nodeId: "0:1")
# 응답이 크면 파일로 떨어지니 들여쓰기로 잘라 본다
```

### 마이페이지 노드가 바뀌었다

`118:291` → `234:567`. 내용은 같아 재작업은 없었고, 코드 주석의 노드 번호만 갱신했다.

## 홈에서 내린 판단들

**아이 전환은 라우트에서 조립한다.** `HomeScreen` 은 story feature 에 있고
`childName` / `childOptions` / `onSelectChild` 만 받는다. 아이 목록은
`src/app/(tabs)/home.tsx` 가 넘긴다 — story 가 child 를 직접 가져오면 feature 끼리
의존이 생긴다. **활성 아이는 아직 라우트의 `useState` 라서 홈을 벗어나면
초기화된다.** 전역 상태가 붙으면 그 자리를 스토어로 바꾸면 된다.

**"이어하기" 버튼 색을 디자인과 다르게 썼다.** 디자인은 `#FFA833`(primary/400)에
Arial 라벨인데, 이 프레임엔 Figma Variable 이 하나도 안 걸려 있고 다른 화면의 주
CTA 는 전부 `#FF9200`(primary/500)이다. 미완성 시안으로 보고 토큰(`primary`)으로
맞췄다. 크기(h49 / 20 Bold)는 디자인대로 `Button size="xl"` 을 새로 만들었다.

**전환 알약 배경색을 뒤집었다.** 디자인은 흰 배경 위 `#F8F9FA` 알약인데, 홈의
페이지 배경이 그 `#F8F9FA` 라 그대로 두면 알약이 안 보인다. 흰 알약으로 바꿔
대비 관계만 유지했다. **바탕이 흰 화면(리포트)에서는 반대로 디자인 원본을 써야
한다** — `ChildSwitcher` 의 `tone` prop 이 그것이다.

**썸네일 그림은 실측(315)대로 두고 잘리게 놔뒀다.** 그림이 썸네일 높이(297)를
넘어서 이야기에 따라 위아래가 잘리는데, 크게 보이는 쪽이 낫다는 판단을 받았다.

**추천 카드는 폭에 따라 썸네일이 줄어든다.** 디자인의 191px 을 분할 화면(768dp)에서
그대로 쓰면 "약 12분"이 세로로 쪼개진다. `useResponsive().select` 로 104/140/191.

**인사말 조사를 계산한다.** 받침이 있으면 "규한아", 없으면 "지오야"
(`HomeScreen` 의 `vocativeParticle`). 디자인엔 "지오야" 하나뿐이라 규칙은 직접 넣었다.

## 대화 화면에서 내린 판단들

### 2026-08-12 에 2단으로 전면 재설계됐다

시안이 바뀌어 다시 만들었다. 이전 노드(`86:410` 도입, `216:277` 미션)는 **삭제**되고
`380:342` / `380:281` 로 교체됐다. `161:1158`(대화)은 ID 만 남고 내용이 전부 바뀌었다.

| 무엇        | 이전                       | 지금                             |
| ----------- | -------------------------- | -------------------------------- |
| 레이아웃    | 배경 전체 + 가로 패널(944) | **2단** (왼쪽 457 : 오른쪽 539)  |
| 줄거리      | 배경 위 좁은 판            | 왼쪽 열을 세로로 꽉 채우는 큰 판 |
| 배경 그림   | 화면 전체                  | 오른쪽 열만 (539×687)            |
| 대화·마이크 | 화면 가운데                | 오른쪽 열 안                     |

**왼쪽 패널은 새 컴포넌트다** (`NarrationPanel`, 380:274). `#FFFBF6` 바탕에 radius 16,
왼쪽 위 뱃지(15/Bold), 오른쪽 아래 "다시 듣기". 미션 패널은 여전히 옛 모양이라
`ScenePanel` 을 그대로 쓴다 — 뱃지 크기도 미션만 예전 것(12)이다.

**비율은 flex 실측값으로 넣었다** (`flex: 457` / `flex: 539`, 사이 13). 오른쪽 열은
화면 오른쪽·아래 끝에 붙으므로 페이지에 오른쪽 여백을 주지 않는다.

**미션·마이크는 한 덩어리로 바닥에 붙인다.** 디자인 좌표를 그대로 재현하려고
`marginTop: 'auto'` + 바닥 여백 23 + 둘 사이 10 으로 잡았다. 미션이 있든 없든
마이크 위치가 시안과 정확히 맞는다(미션 있음 y357, 없음 y481 — 둘 다 계산이 떨어진다).

**말풍선 최대 폭을 520 → 261 로 줄였다.** 대화 열이 944 에서 497 로 좁아졌다.

---

**세 시안은 한 화면의 세 순간이라 하나로 합쳤다.** `StoryPlayScreen` 이
마이크 상태(`blocked` → `ready` → `listening` → `processing`)로 장면을 굴린다.
장면에 미션이 있으면 미션 패널이 하나 더 붙는다.
그 장면은 왼쪽 뱃지도 "이야기 줄거리" → "이야기 상황" 으로 바뀐다 — 디자인이 그렇다.

**말풍선은 임의 위치가 아니었다.** 시안을 열어보니 배경 위 절대좌표가 아니라
평범한 채팅 레이아웃이다(등장인물 왼쪽 정렬, 아이 오른쪽 정렬). 배경 그림은
불투명도 40%(디자인 실측)로 깔고, 글은 전부 불투명한 판 위에만 올라간다.
**`Screen` 의 `backdrop` prop 은 이제 이 화면에서 쓰지 않는다** — 그림이 화면 전체가
아니라 오른쪽 열 안에만 들어가므로 열 안에 직접 깐다.

**시안의 대본을 그대로 옮기지 않았다.** 디자인의 이야기("방귀뀌는 며느리")와 본문이
서비스에 쓸 수 없는 자리표시 문장이다. 화면 구조와 3/8 진행도는 디자인대로 두고,
문장만 목데이터에 이미 있는 아기돼지 삼형제로 8장면을 새로 썼다
(`features/story/model/script.ts`). 3번째 장면의 질문·답변만 디자인 문장 그대로다.
**시안 대본이 정식으로 갱신되면 이 파일만 갈아끼우면 된다.**

**"아직 말할 차례가 아니예요" → "아니에요" 로 고쳤다.** 디자인 맞춤법 오류인데
아이가 글을 배우는 앱이라 그대로 두지 않았다. 디자이너 확인 필요.

**배경 그림은 장면별이 아니라 하나뿐이다.** 시안에 들어 있던 한옥 마당 이미지를
`assets/scenes/hanok-yard.jpg` (1536×1055, JPEG 1MB)로 내보내 모든 장면에 쓴다.
원본 PNG 는 6.4MB 라 저장소에 넣지 않았다. 실제로는 서버가 장면마다 내려줄 값이다.

**나레이션 → 아이 차례 전환은 2.5초 타이머다.** TTS 가 붙으면 재생 완료 콜백으로
바꾼다. 마이크를 눌러 녹음을 멈추면 STT 대신 대본에 적힌 예시 답변이 0.9초 뒤에
받아써진 것으로 친다. 둘 다 `StoryPlayScreen` 상단에 TODO 로 표시해뒀다.

**보내기 버튼은 디자인(h42)보다 크다.** `Button size="lg"`(h48)를 썼다 — 아이
손가락이 닿는 버튼이라 터치 권장치(48dp)를 지키는 쪽을 골랐다.

**시안에 있던 세 점 로딩 아이콘 하나를 뺐다.** 161:1158 의 아이 말풍선 왼쪽에
"너의 말을 듣고 있어"(점 3개 + 문구)와 별도의 `three-dots-loading` 이 둘 다 있는데,
같은 뜻이 두 번 나오고 미션 시안(216:277)에는 문구 쪽만 있다. 문구 쪽만 남겼다.

## 활동 화면에서 내린 판단들

**마이크 상태는 `86:448` 가이드가 기준이다.** 도입·대화 시안이 같은 슬롯의 라벨을
11.3px 과 16px 으로 서로 다르게 그려놨는데, 컴포넌트 스펙 프레임이 15/SemiBold 로
정의하고 있어 그쪽을 따랐다.

**정답을 맞히면 카드를 고른 순서대로 다시 깔아준다.** 디자인(92:1215)도 그렇다 —
흩어진 자리에 번호만 붙어 있으면 "이야기가 이런 순서였구나" 가 안 보인다.

**작은 카드(활동 2/2)의 그림은 디자인에 없어서 넣었다.** 시안은 빈 색면인데,
아이가 카드를 보고 이야기를 떠올리는 게 이 활동의 목적이라 큰 카드와 같은 그림을
44px 로 넣었다.

**활동 2/2 캐릭터 판의 그라디언트는 단색으로 뒀다.** `#FFF8F0` → `#FFEEC2` 세로
그라디언트인데, 이것 하나 때문에 `expo-linear-gradient` 를 새로 넣을 만하지 않다고
봤다. 밝은 쪽 단색(`surfaceAccentWarm`)을 쓴다.

**"홈으로 돌아가기" 는 디자인의 회색 채움 대신 앱의 secondary 버튼(흰 배경 + 테두리)
을 썼다.** 이 프레임도 Arial 라벨이 섞인 미완성 시안이고, 모달들의 보조 버튼과
모양을 맞추는 쪽이 낫다고 판단했다.

**작은 버튼들은 디자인보다 크다.** 보내기(h42)·완료(h38) 모두 `size="lg"`(h48)로
올렸다. 아이 손가락이 닿는 버튼이라 터치 권장치를 지키는 쪽을 택했다.

**활동 카드·핵심 단어·다시 말한 문장은 전부 목데이터다.**
`features/story/model/activity.ts` 한 곳에 모여 있다. 카드 그림 5개는 시안에서
그대로 내보내 `assets/cards/` 에 넣었다.

## 보호자 리포트에서 내린 판단들

**세 프레임은 한 화면이다.** `252:193`(위쪽 + 어휘 탭) · `252:249`(표현·논리 탭
내용) · `252:297`(대표 발화 · 집에서 이어가기 · 리포트 이동)을 `ReportScreen`
하나로 합쳤다. `252:297` 의 폭(내용 959)이 `252:193` 의 스크롤 영역 폭과 정확히
같아서, 768 안에 안 들어가는 부분을 따로 그려둔 것이라고 봤다.

**어휘/표현/논리는 진짜 탭이다.** `252:249` 가 표현과 논리를 한 프레임에 세로로
쌓아 그려놔서 연속 스크롤처럼 보이지만, `252:193` 에 알약 탭 세 개가 어휘만 선택된
상태로 그려져 있다. 캔버스를 아끼려고 두 탭 내용을 나란히 그린 것으로 보고 탭으로
구현했다. 탭이라면 한 번에 하나만 보인다.

**위쪽은 고정, 탭 내용부터 아래만 스크롤한다.** 디자인의 `284:210` 이 overflow-clip
이고 그 안에 스크롤바(`252:415`)까지 그려져 있다. 헤더·이야기 줄·말하기 특징·탭은
고정이고 그 아래만 움직인다. 1024×768 에서 읽는 창이 266px 로 좁지만, 탭을 눌러
비교하는 화면이라 탭이 스크롤을 따라 올라가면 더 쓰기 어렵다.

**화면 바탕이 이 화면만 흰색이다.** 디자인이 `bg-white` 로 그려져 있고, 안쪽
"어휘 피드백"·"선정 이유" 블록이 `#F8F9FA`(앱의 기본 페이지 배경)라 바탕까지 같은
색이면 그 블록이 사라진다. `Screen` 에 `background` prop 을 새로 넣어 이 화면만
`surface` 로 준다.

**그 대가로 아이 전환 알약이 사라졌었다.** `ChildSwitcher` 는 "회색 페이지 위 흰
알약"으로 색을 뒤집어 둔 상태였는데(홈 기준), 이 화면은 바탕이 흰색이라 흰 알약이
그대로 묻혔다. `tone` prop 을 추가해 **바탕이 흰 화면에서는 디자인 원본대로
`#F8F9FA` 알약**을 쓰게 했다. 앞으로 `background="surface"` 인 화면을 만들면
그 위에 올리는 흰색 요소를 전부 다시 봐야 한다.

**이전/다음 리포트 버튼은 앱의 primary·secondary 로 맞췄다.** 디자인은 둘 다 주황
채움인데 이전이 `#FFA833`, 다음이 `#FF9200` 이고 라벨이 Arial 이다 — 홈 "이어하기"
때와 같은 미완성 시안 패턴이다. 모달·완료 화면의 보조 버튼과 모양을 맞춰 이전은
secondary(흰 배경 + 테두리)로 뒀다.

**대신 비활성 색은 디자인(287:273)을 따랐다.** `Button` 의 primary 비활성은 옅은
주황(`#FFC266`)인데, 이건 로그인의 "계속하기"처럼 입력만 채우면 눌리는 버튼 기준이다.
리포트 페이저는 끝에 도달하면 정말로 못 누르는 버튼이라 주황으로 두면 계속 누르게
된다. `Button` 에 `disabledTone="neutral"` 을 새로 넣어 회색으로 내렸다.

**시안의 본문을 그대로 옮기지 않았다.** 디자인 본문이 여전히 "방귀 뀌는 며느리"라
대화 화면 대본과 같은 이유로 아기돼지 삼형제·너구리의 도토리로 새로 썼다
(`features/report/model/types.ts`). 화면 구조와 항목 개수는 디자인 그대로다.
**시안 본문이 갱신되면 이 파일만 갈아끼우면 된다.**

**리포트를 두 편 넣었다.** 한 편만 있으면 이전/다음이 둘 다 비활성이라 페이저가
도는지 확인할 수 없다. 두 편이면 넘어가는 것과 양 끝 비활성을 모두 눌러볼 수 있다.

**이야기 줄의 아이콘이 사람 모양이다.** 이야기 제목 옆인데 하단 탭 '마이페이지'와
같은 벡터를 두 가지 색으로 칠한 것이다(`252:204`). 이야기 그림으로 바꾸고 싶었지만
에셋을 임의로 바꾸지 않는다는 규칙대로 시안 그대로 내보내 썼다
(`assets/icons/profile-fill.svg`). **마이페이지의 집 모양 아이콘과 같은 종류의
디자인 오류로 보인다 — 디자이너 확인 필요.**

**"궁금해한 어휘" 칩이 파란 배경에 갈색 글자다**(`#E7F5FF` + `#4D2500`).
주황 칩에서 글자색만 안 바꾼 것처럼 보이는데, 대비 자체는 충분해서 시안대로 뒀다.
`colors.surfaceInfo` / `colors.textInfo` 로 토큰화했다. 이것도 확인 대상이다.

**디자인에 없어서 직접 처리한 것.** 어휘 칩이 한 줄을 넘치면 다음 줄로 내려가게
했고(시안엔 한 줄뿐), 표현·논리 카드의 인용문은 고정 높이 36 대신 세로 여백으로
바꿔 아이 말이 길어도 잘리지 않게 했다. 이야기 제목은 말줄임 처리했다.

## 알아둬야 할 판단들

**"미완성 시안"이 섞여 있다.** 로그인·이야기 목록처럼 자동레이아웃 + Pretendard +
Variables 로 제대로 짜인 화면이 있고, 모달 몇 개는 절대좌표 + Inter/Arial +
소수점 폰트크기(13.1px 등)로 된 시안이다. 후자는 사용자 승인을 받아 **토큰으로
보정**했다(Pretendard 로 바꾸고 크기를 정수화). 그쪽 디자인이 갱신되면 다시 맞춰야 한다.

**디자인 오류로 보이는 것.** 마이페이지 "학습 리포트 보기" 아이콘이 집 모양이다
(하단 탭 '홈'과 같은 에셋). 디자인 자체가 그러해서 그대로 구현했다. 디자이너 확인 필요.

**빈 상태·긴 텍스트·다중 항목 처리는 디자인에 없어서 직접 만들었다.**
`EmptyState` 문구, 아바타 줄바꿈, 이름 말줄임 등. 시안이 나오면 교체 대상이다.

**리포트·계정 설정에만 보호자 확인을 걸었다.** 어느 메뉴에 걸지는 명시가 없었다.
도움말은 열어뒀다. 리포트는 이제 확인을 통과하면 `/report` 로 실제로 넘어간다
(계정 설정은 아직 갈 화면이 없다).

## 검증은 이렇게 했다

새 화면을 붙였으면 아래를 돌리는 게 이 프로젝트의 기본 루프다.

1. `npm run typecheck && npm run lint`
2. 에뮬레이터에 올려 실제로 눌러본다 (`adb shell am start -a android.intent.action.VIEW -d "goodquestion://<route>"` 로 라우트 직행)
3. 수치가 중요하면 `npm run web` 으로 띄우고 브라우저 콘솔에서 `getComputedStyle` 로
   재서 Figma 실측값과 대조한다 — 눈으로 보는 것보다 훨씬 정확하다. 실제로 이 방법으로
   인풋·버튼 높이가 3px 어긋난 걸 찾았다.
4. 긴 이름·많은 항목·빈 목록을 목데이터에 넣어 한 번 흔들어본다. 여기서 레이아웃이
   깨진 적이 여러 번 있다.

`adb shell input text` 는 한글을 넣지 못한다. 한글 입력이 필요하면 딥링크로 우회한다.

### 전체 점검 한 번 돌린 결과 (2026-08-09)

퍼블리싱한 화면을 한 번에 훑을 때 아래를 봤다. 다음에도 같은 걸 보면 된다.

- 전 라우트(17개)를 `history.pushState` 로 순회하며 렌더 여부·가로 넘침·버튼 수
- 버튼마다 **중첩 여부**와 **접근성 이름**. 눌림 영역이 겹치면 스크린리더가 두 개를
  겹쳐 읽고 웹에서는 `button` 안 `button` 이 된다 (단어장 카드에서 실제로 나왔다)
- 없는 id(`/story/999/...`)와 대본 없는 이야기(`/story/1/...`)로 들어가 보기 —
  막다른 길이 생기기 쉽다
- 1280×800 / 1024×768 / 768dp 세 폭에서 가로 넘침
- `grep` 으로 하드코딩 색·폰트, feature 간 import, 미사용 에셋·토큰

이때 고친 것: 단어장 카드의 중첩 눌림 영역, 활동 2/2 빈 상태의 뒤로가기 부재,
완료 화면이 대본 없는 이야기에도 "0번·0개" 축하를 띄우던 것, 카테고리 칩 터치 영역,
`rgba` 리터럴 3개를 토큰으로.

### 리포트 화면 점검 (2026-08-11)

같은 항목을 리포트에도 돌렸다. 세 폭 모두 가로 넘침 0, 중첩 눌림 영역 0,
이름 없는 버튼 0, 콘솔 오류 없음. 타이포는 `getComputedStyle` 로 재서 시안과
대조했다(제목 24 Bold / 이야기 17 SemiBold / 특징 16 Bold `#E07D00` / 대표 발화
20 Bold / 주제 14 Regular, 전부 Pretendard).

마이페이지 → 학습 리포트 보기 → 보호자 확인 → `/report` 까지 실제로 눌러 확인했고,
탭 전환·리포트 앞뒤 이동·양 끝 비활성까지 동작한다.

**주의**: `.expo/types/router.d.ts` 는 dev 서버가 만드는 캐시라, 새 라우트를 추가한
직후에는 `npm run typecheck` 이 "not assignable to parameter of type ..." 로 깨진다.
코드 문제가 아니다. 서버를 한 번 띄우면(`npm run web`) 타입이 다시 생성된다.

### 전체 화면 ↔ 디자인 수치 대조 (2026-08-11)

"구현하면서 디자인과 벌어졌을 것"이라는 지적을 받고 퍼블리싱한 화면 전부를
Figma 현재 지오메트리와 1:1로 다시 맞췄다.

먼저 **디자인이 바뀌었는지**를 봤다. `182:303` 전체를 훑어 `234:567`(직전 작업의
최대 노드) 이후에 생긴 노드를 뽑으니 전부 보호자 리포트와 그 래퍼 섹션뿐이었다.
즉 **기존 화면의 시안은 그대로**고, 벌어진 건 구현 쪽이었다.

찾아서 고친 것 6건:

| 무엇                       | 디자인    | 고치기 전       | 원인                                                                 |
| -------------------------- | --------- | --------------- | -------------------------------------------------------------------- |
| 페이지 좌우 여백 (전 화면) | 24        | 20              | `Screen` 의 `select` 에 `expanded` 키가 없어 `medium`(20)으로 떨어짐 |
| 단어장 단어 카드 높이      | 279       | 152 (내용 높이) | 줄에 높이를 안 잡아 카드가 납작해짐                                  |
| 이야기 목록 본문 여백      | 48        | 20              | 이 화면만 본문이 한 겹 더 들어가 있는 걸 놓침                        |
| 홈 좌우 칸 비율            | 484 : 444 | 468 : 468       | 양쪽 `flex: 1` 로 균등 분할                                          |
| 이야기 상세 시작 버튼 높이 | 56        | 48              | `Button size="lg"` 를 그대로 씀                                      |
| 마이페이지 아이 카드 간격  | 12        | 16              | `spacing.xl` 을 씀                                                   |

**비율은 `flex` 에 실측값을 그대로 넣는다.** 홈은 `flex: 484` / `flex: 444`,
이야기 상세는 `flex: 524` / `flex: 428`. `1.2` 같은 어림수를 쓰면 근거가 사라진다.

고친 뒤 다시 재서 **디자인과 좌표까지 일치**하는 걸 확인했다 (1024×768 기준):

- 이야기 상세 썸네일 524×402 @x24, 시작 버튼 428×56 @x572
- 홈 두 칸 484 @x24 / 444 @x556
- 마이페이지 아이 카드 317×66 @x24, 간격 12
- 활동 카드 160×200 @x84 간격 14, 대화 패널 944 @x40, 완료 버튼 200×52 @x306·x518
  (이 셋은 원래부터 맞았고, 여백을 고쳐도 가운데 정렬이라 그대로다)

**측정할 때 등장 애니메이션을 조심할 것.** `Appear` 의 `from="scale"` 이 끝나기 전에
재면 폭이 0.92배로 나온다. 실제로 회원가입·아이생성 카드가 386/570 으로 나와
어긋난 줄 알았는데, 2.5초 기다렸다 다시 재니 420/620 으로 정확했다. **최소 2초는
기다린 뒤 잰다.**

**웹에서 잰 값은 세로 스크롤바(약 15px)만큼 좁게 나온다.** 안드로이드는 스크롤바가
겹쳐 그려져서 이 차이가 없다. 이야기 목록 카드가 웹에서 294, 기기에서 299 인 이유다.

**남겨둔 것**: 1280 처럼 1024 보다 넓은 화면에서는 본문이 976 이 아니라 1024 로
잡힌다 (`Screen` 의 `maxContentWidth` 가 본문 폭이고 여백은 그 바깥에 붙는다).
1280 에서는 좌우에 128px 씩 배경이 남아 여백이 충분해 그대로 뒀다.

### 태블릿 실기기에서만 나온 결함 2건 (2026-08-11)

위 수치 대조는 전부 웹(`npm run web`)에서 했는데, **에뮬레이터에 올리니 웹에서는
드러나지 않는 게 두 개 더 나왔다.** 수치만 재고 끝내지 말 것.

**1. 하단 탭 바만 화면 끝까지 늘어났다.** 본문은 `maxContentWidth`(1024)로 묶이는데
`(tabs)/_layout.tsx` 의 탭 바에는 상한이 없어서, 1280dp 태블릿에서 탭 바만 1232dp 로
퍼졌다. 같은 화면의 본문(1024)보다 208dp 넓다. 리포트는 탭 바를 화면 안에서 직접
그려 1024 였기 때문에 **화면끼리 탭 바 폭이 달랐다.** 탭 바에도 같은 상한을 걸었다.
1024 폭에서는 둘 다 976 이라 웹에서는 절대 안 보인다.

**2. 보호자 확인 모달이 키보드에 통째로 가렸다.** 인풋과 취소·확인 버튼이 전부
키보드 뒤로 들어가 아무것도 못 눌렀다. `Screen` 은 `useKeyboardInset` 으로 처리하고
있었지만 `Modal` 은 빠져 있었다. 모달 배경에 같은 인셋을 줘서 카드를 위로 올렸다.
**입력이 있는 모달은 `Screen` 규칙(AGENTS.md)이 적용되지 않는다는 걸 놓쳤던 것이다.**

### 에뮬레이터를 하나 더 띄울 때 (다른 프로젝트와 같이 쓸 때)

다른 에뮬레이터가 이미 돌고 있으면 아래를 지킨다. 실제로 한 번 겪었다.

- `emulator -port 5556` 으로 포트를 직접 지정하고, **모든 adb 명령에 `-s emulator-5556`
  을 붙인다.** 안 붙이면 상대 기기로 간다. `adb kill-server` 는 절대 쓰지 않는다.
- Metro 포트가 겹치는지 `netstat` 으로 먼저 본다. `adb reverse` 는 기기별이라
  서로 다른 호스트 포트를 봐도 충돌하지 않는다.
- **`-no-snapshot-save` 로 띄운다.** 종료할 때 새 상태를 쓰지 않는다. 다만 이걸
  줘도 **실행하는 순간 그 AVD 의 기존 퀵부트 스냅샷은 무효화된다**(`snapshot.pb` 가
  11바이트로 잘린다). 남의 AVD 를 빌려 쓰면 다음번에 콜드 부팅하게 되니, 가능하면
  전용 AVD 를 새로 만들어 쓰는 게 낫다. 설치한 앱도 `userdata-qemu.img.qcow2` 에
  그대로 남으므로 끝나면 `adb -s <serial> uninstall` 로 지운다.
- **좌표로 탭하기 전에 반드시 스크린샷을 먼저 찍어 우리 앱이 전면인지 확인한다.**
  다른 앱의 잔여 태스크(설정 화면 등)가 앞에 떠 있을 수 있다.
- 딥링크는 앱이 이미 전면일 때는 라우팅되지 않는다. 화면 안의 탭 바를 누르거나,
  `am force-stop` 후 딥링크로 다시 띄운다.
- 화면 확인은 `adb -s <serial> exec-out screencap -p > a.png`.

## 백엔드 연동 (2026-08-15 시작 — 여기부터가 다음 작업이다)

### 명세는 어디에 있나

Notion **"굿퀘스천 API 명세서"** (`3bd5ac70-2736-80c7-a03c-c33263ec861f`).
도메인별 인라인 데이터베이스 7개로 나뉘어 있고, 각 행이 엔드포인트 하나다.
Notion MCP 로 읽는다 — 목록은 `query_data_sources`, 상세는 `fetch`.

**중요: `/api` prefix 를 붙이지 않는다. 명세 상세 페이지가 틀렸다.**

데이터베이스의 `api path` 속성(`/auth/sync-profile`)과 상세 페이지의
EndPoint(`/api/auth/sync-profile`)가 엇갈리는데, **2026-08-15 에 배포 서버를 직접
찔러 속성 쪽이 맞다는 걸 확인했다.**

```
POST /auth/sync-profile      → 401  (엔드포인트가 있고 Bearer 를 요구한다)
POST /api/auth/sync-profile  → 404
GET  /users/me · /vocabulary · /sessions/{id}/post-activity → 401
GET  /api/... 는 전부 404
```

`GET /main` 은 양쪽 다 404 라 아직 미구현으로 보인다. **명세의 EndPoint 표기를
고쳐달라고 백엔드에 요청할 것.**

그리고 401 이 돌아온다는 건 **여러 엔드포인트가 이미 매핑돼 있다는 뜻이다** —
명세의 `BE 진행상황` 칸이 비어 있는 것보다 실제로는 더 진행돼 있다.

### 엔드포인트 전체 (23개)

| 그룹                     | 엔드포인트                                                                                                                          |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------- |
| 인증 `/api/auth`         | `POST /sync-profile` · `POST /verify-password` · `DELETE /me`                                                                       |
| 유저·자녀 `/api/users`   | `GET·PATCH /me` · `GET /mypage` · `GET·POST /me/children` · `PATCH /me/children/{child_id}` · `POST /profile-image/presigned-url`   |
| 단어장 `/api/vocabulary` | `GET /` · `GET /{vocab_id}` · `POST·DELETE /{vocab_id}/save`                                                                        |
| 메인·이야기              | `GET /api/main` · `GET /api/stories`                                                                                                |
| 학습 `/api/progress`     | `POST /{story_id}/start` · `GET /active` · `GET /{story_id}` · `POST /{story_id}/steps/{step_index}` · `.../complete` · `.../speak` |
| 활동 `/api/sessions`     | `GET /{session_id}/post-activity` · `POST .../submit` · `POST .../retell`                                                           |

### 인증은 Supabase JWT 다 — 이게 제일 큰 제약

명세의 권한 칸이 `SUPABASE_ONLY (Supabase JWT 검증만)` / `PARENT (부모 계정, DB
프로필 등록 필요)` 로 되어 있다. **로그인은 서버가 아니라 Supabase 가 처리하고,
우리 서버는 그 JWT 를 검증만 한다.** 따라서:

- **이메일/비밀번호 로그인 API 가 명세에 없다.** 서버에 붙일 게 아니라
  `@supabase/supabase-js` 로 직접 로그인해야 한다. 이메일/비밀번호와 소셜 로그인
  **둘 다 쓴다**(사용자 확인 완료)
- `POST /api/auth/sync-profile` 은 회원가입이 아니라 **Supabase 로그인 뒤 서버 DB 에
  프로필을 만드는 단계**다. body `{ name }`, 성공 201, 이미 있으면 409
- 소셜 로그인의 복귀 주소로 앞서 정리한 딥링크(`goodquestion://oauth`)를
  **Supabase 콘솔의 Redirect URL 에 등록**해야 한다

**앱은 서버 두 곳에 붙는다.** 이걸 헷갈리면 설계가 꼬인다.

```
① 로그인   앱 → Supabase(supabase.co)     : 이메일/비번 또는 소셜 → JWT 발급
② 그 외    앱 → 우리 백엔드(api base url) : Authorization: Bearer <①의 JWT>
```

우리 백엔드는 토큰을 **발급하지 않고 검증만** 한다(백엔드 env 에 `SUPABASE_JWT_SECRET`
이 있는 이유). 그래서 base url 하나로는 로그인할 데가 없다. 하나로 줄이려면 백엔드가
`POST /api/auth/login` 프록시를 만들어야 하는데, 소셜까지 직접 구현해야 해서
지금 방식(앱 → Supabase 직접)이 일반적이다.

**Confirm email 을 켜둔 채로 간다** (사용자 결정). 그래서 `signUp()` 직후에는 세션이
없다(`{ user, session: null }`). `sync-profile` 은 Bearer 가 필수라 **가입 직후가 아니라
"메일 확인 뒤 첫 로그인 성공 시점"에 불러야 한다.** 이걸 놓치면 Supabase 엔 계정이
있는데 서버 DB 엔 프로필이 없어 401 "프로필이 등록되지 않은 사용자입니다" 가 난다.

### Supabase 연결은 끝났다 (2026-08-15)

`.env` 에 세 키가 다 들어가 있다 — `EXPO_PUBLIC_API_BASE_URL`,
`EXPO_PUBLIC_SUPABASE_URL`, `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
`auth/v1/health` 가 200 이고 잘못된 자격증명에 `invalid_credentials` 가 오는 것까지
확인했다. **인증 서버에 정상적으로 닿는다.**

**`EXPO_PUBLIC_SUPABASE_URL` 은 프로젝트 루트여야 한다.** 처음에 콘솔의 Data API
주소(`https://xxx.supabase.co/rest/v1/`)가 들어가 있어서, 로그인 요청이 auth 서버가
아니라 PostgREST 로 가 `PGRST125 Invalid path specified in request URL` 로 실패했다.
`Project Settings → API → Project URL` 값을 넣어야 한다. 같은 실수를 또 하면
`env.ts` 가 개발 중에 경고를 띄운다(끝 슬래시는 자동으로 떼낸다).

**앱에 절대 넣지 않는 값**: `SUPABASE_JWT_SECRET`, `SUPABASE_SERVICE_ROLE_KEY`.
둘 다 백엔드 전용이다. anon key 만 클라이언트용이다.

### 로그인·회원가입을 붙였다 (2026-08-15)

화면 두 개가 실제로 Supabase 에 붙었다. 우리 백엔드에는 로그인 엔드포인트가
없으므로 인증은 전부 Supabase 로 간다.

| 무엇             | 어디                                            |
| ---------------- | ----------------------------------------------- |
| 이메일 로그인    | `features/auth/hooks/useEmailLogin.ts`          |
| 회원가입 3단계   | `features/auth/hooks/useSignUp.ts`              |
| 서버 프로필 생성 | `features/auth/api/ensureServerProfile.ts`      |
| 인증 에러 → 문구 | `features/auth/model/authErrors.ts`             |
| 보호자 이름 결정 | `features/auth/model/profileName.ts`            |

**회원가입은 `signUp()` 이 아니라 OTP 흐름이다.** 시안의 4단계(이메일 → 인증코드
6자리 → 비밀번호 → 완료)를 살리려면 이 방법뿐이다 (사용자 결정).

```
1단계  signInWithOtp({ shouldCreateUser: true })  → 메일로 6자리 코드, 계정 생성
2단계  verifyOtp({ type: 'email' })               → 코드 확인. 여기서 세션이 생긴다
3단계  updateUser({ password }) + sync-profile    → 비밀번호 설정, 서버 프로필 생성
```

`signUp({ email, password })` 을 쓰지 않은 이유가 둘이다. 시안은 비밀번호를
**코드 확인 뒤에** 받는데 `signUp` 은 그보다 먼저 필요하고, Confirm email 이
켜져 있으면 `signUp` 직후에 세션이 없어(`{ user, session: null }`) `sync-profile`
을 부를 수가 없다. OTP 는 2단계에서 세션이 생겨 **가입 자리에서 프로필까지 끝난다.**

**메일 발송은 해결됐다 (2026-08-15).** 커스텀 SMTP 와 Magic Link 템플릿의
`{{ .Token }}` 이 적용돼 인증코드가 실제로 도착한다.

**⚠️ 코드가 6자리가 아니라 8자리다.** Supabase 콘솔의 **Email OTP Length** 가 8 로
맞춰져 있다. 시안(86:683)은 6칸으로 그려져 있지만 **화면이 서버를 따라가야 해서
8칸으로 고쳤다** (`CODE_LENGTH = 8`, 회원가입·비밀번호 찾기 둘 다).

칸 폭도 같이 손봤다. 디자인 실측 44 를 8칸에 그대로 쓰면
`44×8 + 간격 7 = 408` 이라 카드 안쪽 폭(420 − 좌우 32 = 356)을 넘친다.
`OtpInput` 의 칸을 **남는 폭을 나눠 갖고 44 에서 멈추게**(`flex: 1` + `maxWidth: 44`)
바꿨다 — **6자리로 되돌려도 디자인 그대로 44 가 나온다.**

**디자이너 확인 대상**: 시안의 코드칸을 8개로 맞출지, 아니면 콘솔을 6으로 내릴지.

**`sync-profile` 의 `name` 은 추론한 값이다.** 시안의 회원가입에 이름칸이 없는데
명세는 `{ name }` 을 요구한다. 그래서 `user_metadata`(소셜이 채워준다) → 이메일의
@ 앞부분 순으로 고른다(`profileName.ts`). **가입 단계에 이름칸을 넣을지 디자이너
확인이 필요하다.**

**`sync-profile` 실패는 지금 로그인을 막지 않는다.** BE 가 미구현이라
network/404/5xx 는 경고만 남기고 통과시킨다. 409(이미 있음)는 정상 경로다.
401/403 은 토큰이 거부된 것이라 Supabase 세션까지 되돌린다.
**BE 가 붙으면 `ensureServerProfile` 의 그 분기를 지워 실패로 취급해야 한다.**

**에러 문구는 `error.code` 로 가른다.** `message` 문자열로 분기하면 Supabase 가
문구를 바꿀 때 조용히 깨진다. 로그인 실패는 이메일/비밀번호를 구분해 말하지
않는다(계정 존재 여부가 새어나간다).

**어디까지 확인했나.** 웹(1024×768)에서 잘못된 자격증명 → `400 invalid_credentials`
→ "이메일 또는 비밀번호가 올바르지 않습니다."가 뜨고 입력을 고치면 사라지는 것,
가입 1단계에서 형식이 틀린 주소 → `validation_failed` → 문구가 뜨고 2단계로
넘어가지 않는 것까지 실제 서버 상대로 확인했다. 앱 콘솔 오류는 없다.
**로그인 성공 경로와 실제 코드 수신은 아직 못 돌렸다** — 아래가 필요하다.

### ⚠️ Supabase 프로젝트가 우리 계정 소유가 아니다 (2026-08-15에 확인)

> **이 절의 메일 문제는 해결됐다.** 팀원이 커스텀 SMTP 와 템플릿을 적용해서
> 지금은 인증코드가 정상 도착한다. 아래 내용은 **왜 그동안 안 왔는지**와
> 프로젝트 소유 관계를 남겨두는 기록이다 — 콘솔을 만져야 할 일이 생기면
> 우리가 아니라 팀원이 해야 한다는 점은 그대로다.


**인증 메일이 우리 쪽으로는 오지 않는다.** 실제로 `abccba111307@gmail.com` 로
가입 코드를 보내봤는데 **API 는 200 을 주고 화면은 2단계로 넘어갔지만 메일은
스팸 포함 한 통도 오지 않았다.**

원인: `.env` 의 프로젝트(`vytcszxecrvdbwaixrhi`)가 이 Gmail 계정의 것이 아니다.
콘솔에 로그인해 `/dashboard/project/vytcszxecrvdbwaixrhi/...` 로 들어가면 조직
목록으로 튕긴다. 이 계정이 가진 조직은 AI-Dev / Earmark / SpainBody 셋인데
어디에도 그 프로젝트가 없다 — **팀원 소유다.**

Supabase 의 기본(내장) 메일 서비스는 **프로젝트 조직 멤버에게만 발송한다**
(게다가 시간당 2통 제한이다). 조직 밖 주소로 보내면 API 는 성공을 돌려주고
메일만 조용히 버려진다. 지금 우리가 본 게 정확히 그 모양이다.

**즉 코드 방식 가입은 우리 계정만으로는 절대 테스트할 수 없다.** 아래 중 하나가
필요하고, 전부 프로젝트 소유자(팀원)만 할 수 있다.

| 방법                                                | 풀리는 것                          |
| --------------------------------------------------- | ---------------------------------- |
| 그 조직에 `abccba111307@gmail.com` 을 멤버로 초대   | 메일이 오게 된다. 제일 간단하다    |
| 커스텀 SMTP 를 붙인다                               | 멤버 제한·시간당 2통 제한이 없어진다 |
| 대시보드에서 Auto Confirm 로 계정을 만들어 비번 공유 | **로그인** 경로만 바로 검증 가능    |

마지막 줄이 지금 제일 빠른 길이다. 로그인은 메일이 아예 필요 없다.

**팀원에게 정리해서 물어볼 것** (한 번에 부탁하는 게 낫다).

1. Authentication → Emails → **Magic Link 템플릿에 `{{ .Token }}` 추가** —
   지금은 기본값이라 메일에 링크만 오고 6자리 코드가 안 보인다. 이게 없으면
   2단계 화면을 쓸 수 없다
2. 조직에 우리 계정 초대, 또는 확인 처리된 테스트 계정 하나 + 비밀번호
3. 소셜 provider 중 무엇이 Enabled 인지
4. `goodquestion://oauth` 가 URL Configuration 에 등록돼 있는지

**주의**: 위 테스트로 `abccba111307@gmail.com` 이 그 프로젝트에 **미확인 상태로
생성됐을 가능성이 있다**(`shouldCreateUser: true`). 팀원에게 Users 목록 확인을
같이 부탁하고, 필요 없으면 지워달라고 하면 된다.

### 실기기 태블릿에서 확인한 것 (2026-08-15)

SM-T510 (`R52M90BNTBD`) 에 올려 로그인 화면을 실제로 눌러봤다. 화면은 가로로
정상 렌더되고, 잘못된 자격증명 입력 → 양쪽 인풋 테두리 빨강 + 비밀번호 아래
문구까지 디자인대로 나온다. IME 의 '완료' 키가 `onSubmitEditing` 으로 제출까지
연결된다(키보드가 내려가면서 "계속하기" 버튼도 가려지지 않는다).

**태블릿에 Wi-Fi 를 연결해야 인증을 끝까지 볼 수 있다.** 지금 기기는 인터넷이
없다(`dumpsys connectivity` → `Active default network: none`, `ping` → Network is
unreachable). Metro 는 USB `adb reverse` 로 도는 거라 앱은 멀쩡히 뜨는데
Supabase 에는 못 닿아서 "네트워크에 연결할 수 없습니다" 로 떨어진다.
**앱 문제가 아니다** — 오히려 `authErrorMessage` 의 network 분기가 실기기에서
제대로 동작한다는 확인이 됐다.

#### ⚠️ 이 태블릿은 armeabi-v7a (32비트) 다

기존 APK 는 x86_64(에뮬레이터용)라 `INSTALL_FAILED_NO_MATCHING_ABIS` 로 안 들어간다.
`npx expo run:android` 를 **태블릿만 연결한 상태로** 돌리면 기기 ABI 를 감지해
armeabi-v7a 로 빌드한다(첫 빌드 7분). 에뮬레이터가 같이 붙어 있으면 엉뚱한 ABI 로
빌드하니 주의.

```bash
npx expo run:android --no-bundler   # 빌드 + 설치 (네이티브 변경 시에만)
adb reverse tcp:8081 tcp:8081
adb shell am force-stop com.goodquestion.app
adb shell am start -a android.intent.action.VIEW \
  -d "exp+good-question-app://expo-development-client/?url=http%3A%2F%2Flocalhost%3A8081"
```

**Metro 가 완전히 뜨기 전에 앱을 열면** `java.net.SocketException: Connection reset`
화면이 뜬다. 코드 문제가 아니라 순서 문제다 — 서버를 먼저 띄우고 앱을 다시 연다.

**빌드를 중간에 끊었다면 `expo-constants` 캐시를 지워야 한다.** 실제로 겪었다.

```
Execution failed for task ':expo-constants:mergeDebugAssets'.
> java.nio.file.NoSuchFileException:
  node_modules/expo-constants/android/build/generated/assets/expo-constants/app.config
```

끊긴 빌드가 생성 에셋만 지우고 Gradle 에는 "최신"으로 남겨둬서, 다음 빌드가
없는 파일을 그대로 쓰려다 터진다. `gradlew clean`(전체 재빌드)까지 갈 것 없이
해당 디렉터리만 지우면 된다.

```bash
rm -rf node_modules/expo-constants/android/build
```

`app.json` 의 plugins 를 고친 뒤에도 이 에셋이 다시 생성돼야 하므로, 플러그인을
추가했는데 반영이 안 되는 것 같으면 같은 자리를 의심한다.

`adb shell input text` 는 한글을 못 넣지만 이메일·비밀번호는 ASCII 라 그대로 된다.

### 라우트 가드를 붙였다 (2026-08-15)

세션이 없으면 안쪽 화면이 열리지 않는다. 그동안 딥링크로 아무 화면이나 열렸는데,
편했던 만큼 **남의 아이 리포트로 곧장 들어갈 수 있다는 뜻**이기도 했다.

`features/auth/components/AuthGate.tsx` 가 `_layout.tsx` 에서 `Stack` 을 감싼다.
규칙은 두 줄뿐이다.

1. 세션 없음 + 보호 화면 → 로그인으로 되돌린다 (항상)
2. 세션 있음 + 로그인 화면 → 건너뛴다 (**앱을 켠 직후 한 번만**)

**2번을 한 번만 하는 게 핵심이다.** 매번 걸면 로그인 도중 세션이 잠깐 생겼다
사라지는 경우(`ensureServerProfile` 이 토큰 거부로 `signOut` 하는 경우)에 화면이
튀었다 돌아오면서 **에러 문구가 사라진다.** 그래서 로그인 성공 뒤 이동은
`LoginScreen` 이 직접 하고, 가드는 앱 시작 시점만 본다.

공개 라우트는 `PUBLIC_ROUTES` 에 모아뒀다 — 로그인(segment 없음)·`signup`·
`find-password`·`dev`·`_sitemap`·`+not-found`. **`dev` 갤러리는 열어뒀다**(개발
편의). 프로덕션 빌드에서 이 라우트를 빼는 건 별개 문제로 남아 있다.

세션을 읽는 동안(`isLoading`)은 아무것도 그리지 않는다. 여기서 children 을 그리면
로그인된 사용자에게 **로그인 화면이 한 프레임 번쩍인다.**

**로그아웃이 이제 실제로 세션을 지운다.** 예전엔 `router.replace('/')` 로 화면만
옮겼는데, 그러면 저장된 세션이 남아 딥링크로 다시 들어올 수 있었다. 지금은
`supabase.auth.signOut()` 만 부르고 이동은 가드에 맡긴다.

#### 실기기에서 확인한 것

Wi-Fi 를 연결한 뒤 태블릿에서 확인했다.

| 시험                                     | 결과                                    |
| ---------------------------------------- | --------------------------------------- |
| `goodquestion://report` (보호, 세션 없음) | 로그인 화면으로 되돌아감 ✓              |
| `goodquestion://signup` (공개) — **대조** | 정상 진입 ✓                             |
| 잘못된 자격증명으로 로그인               | "이메일 또는 비밀번호가 올바르지 않습니다" ✓ |

**대조 시험이 중요했다.** 딥링크는 앱이 이미 전면일 때 무시되는 경우가 있어서,
`report` 가 막힌 게 가드 때문인지 딥링크가 안 먹은 건지 구분이 안 된다.
공개 라우트가 같은 조건에서 정상 진입하는 걸 보여야 가드가 막았다고 말할 수 있다.

### API 연동 진행 상황 (2026-08-15)

명세 순서대로 붙이는 중이다. **테스트 계정이 없어 어느 것도 실제 응답으로는
확인하지 못했다** — 타입·경로·캐시 설계까지만 맞춰 뒀다.

| 그룹      | 상태                                    | 어디                                    |
| --------- | --------------------------------------- | --------------------------------------- |
**전부 화면까지 연결됐다. 앱에 목데이터가 하나도 남아 있지 않다.**

| 인증      | ✅ 3개 전부 |
| 유저·자녀 | ✅ |
| 단어장    | ✅ |
| 이야기    | ✅ 목록·홈·상세 |
| 학습      | ✅ 8개 전부 (대화 화면) |
| 활동      | ✅ 3개 전부 |
| 리포트    | ✅ 조회·생성 |

**명세 29개 중 28개를 코드로 옮겼고, 화면까지 전부 연결했다.**
안 옮긴 하나는 `GET /users/mypage` 인데 쓰지 않기로 한 것이다(아이 목록 출처를
하나로 두려고 — `accountApi.ts` 주석). 훅만 있고 붙일 화면이 없는 게 둘 있다
(`PATCH /users/me` 는 계정 설정 화면 대기, `PATCH /users/me/children/{id}` 는
아이 수정 화면 대기).

**실제 서버 응답으로 검증된 건 아직 0개다.** 유일하게 실호출된
`POST /auth/sync-profile` 은 백엔드 터널이 끊긴 상태(530)라 5xx 를 받았다.
**서버가 올라오면 화면을 한 번씩 눌러보는 게 다음 할 일이다.**

#### 아이 목록이 서버에서 온다

`GET /users/me/children` 이 목록을, `GET /users/me` 가 보호자 이름·이메일을 준다.
`MOCK_CHILDREN` 은 지웠다.

**활성 아이를 전역 스토어로 올렸다** (`features/child/model/activeChildStore.ts`).
그전에는 홈·리포트가 각자 `useState` 라 홈에서 고른 아이가 리포트로 이어지지
않았다. **id 만 저장한다** — 이름·나이는 서버 값이라 쿼리 캐시에 그대로 둔다.
꺼내 쓸 때는 `useActiveChild()` 하나면 된다.

**`GET /users/mypage` 는 쓰지 않는다.** 보호자와 아이를 한 번에 주는 편의
엔드포인트인데, 그러면 아이 목록이 두 군데서 와서 추가할 때 캐시 두 개를
맞춰야 한다. 요청 한 번보다 **출처 하나**를 택했다 (이유는 `accountApi.ts` 주석).

**아바타를 `profile_image_url` 에 실어 보낸다.** 디자인은 기본 아바타 8종 중
고르게 하는데 서버 스키마엔 URL 칸뿐이고 아바타 필드가 없다. 아바타 그림은
번들 SVG 라 올릴 URL 자체가 없어서 `avatar:bear` 같은 표식을 넣는다
(`features/child/model/avatarRef.ts`). **백엔드에 `avatar_id` 필드를 요청하는 게
맞다** — 그때 이 파일만 고치면 화면은 그대로다.

#### 단어장이 서버에서 온다

`GET /vocabulary?child_id=` / `GET /vocabulary/{id}?child_id=` /
`POST·DELETE /vocabulary/{id}/save?child_id=`.

**전부 `child_id` 가 필수다.** 그래서 `WordbookScreen` 과 `WordDetailScreen` 이
`childId` 를 prop 으로 받고 라우트가 활성 아이를 넘긴다 (wordbook 은 child 를 모른다).

**하트는 낙관적 갱신이다.** 아이가 눌렀는데 왕복이 끝날 때까지 반응이 없으면
고장난 줄 알고 계속 누른다. 먼저 채워 보여주고 실패하면 되돌린다.
**저장 시 409 는 성공으로 친다** — 빠르게 두 번 누르면 실제로 난다.

**단어 카드의 아이 뱃지는 이제 prop 이다.** 목록이 이미 `child_id` 로 걸러져
있어서 한 화면의 단어는 전부 같은 아이 것이다.

#### 목데이터를 전부 걷어냈다 (2026-08-16)

`MOCK_STORIES` · `MOCK_PROGRESS` · `MOCK_REPORTS` · `script.ts` · 활동 목데이터를
전부 지웠다. **이제 화면에 보이는 값은 전부 서버에서 온다.**

그 대가로 **서버가 꺼져 있으면 화면 대부분이 빈 상태로 보인다.** 지금이 그 상태다
(터널 530). 앱이 고장난 게 아니다.

번들 그림(`assets/stories`, `assets/cards`)은 지우지 않았다. 서버가 그림을 안 주는
이야기가 있을 수 있어서 컴포넌트들이 `imageUrl` → 번들 `Icon` 순으로 떨어지게
해뒀다. 다만 **지금은 어느 카드도 번들 그림을 지정하지 않는다** — 서버 id 와
매칭할 방법이 없기 때문이다. 서버가 표지를 안 주면 색면만 남는다.

#### 이야기 상세는 디자인 블록 세 개가 빠져 있다

`GET /stories/{id}` 가 없어서 **줄거리 · 역할 안내 · 등장 인물**을 채울 데가 없다.
빈 카드를 남기면 로딩이 안 끝난 것처럼 보여서 통째로 뺐다. 지금 상세 화면은
목록이 주는 값(제목 · 주제 태그 · 예상 시간 · 표지)만 보여준다.
**상세 엔드포인트가 생기면 `StoryDetailScreen` 의 그 자리에 되살린다.**

#### 이야기는 API 계층만 만들고 화면은 그대로 뒀다 (해소됨)

`features/story/api/` 에 `GET /stories?topic=` 과 `GET /main?child_id=` 를
붙여 뒀지만 **화면은 아직 `MOCK_STORIES` 를 쓴다.** 바꾸지 못한 이유가 셋이다.

1. **`GET /stories/{id}` 상세가 명세에 없다.** 목록은 제목·시간·주제·표지뿐이라
   상세 화면의 줄거리·역할 안내·등장인물을 채울 데가 없다
2. **표지 그림의 출처가 다르다.** 지금은 Figma 에서 내보낸 번들 SVG 인데 서버는
   `thumbnail_url` 을 준다. 바꾸면 서버에 그림이 없을 때 카드가 빈 칸이 되는데,
   확인할 계정이 없어 지금은 검증할 방법도 없다
3. **id 체계가 다르다.** 앱은 '1'~'6', 서버는 UUID 다. 목록만 서버로 바꾸면
   상세·대본(`script.ts`)·활동(`activity.ts`) 목데이터가 전부 매칭에 실패한다

1번이 풀리면 셋을 한꺼번에 바꾸는 게 맞다. **표지를 서버 이미지로 바꿀지는
디자인 판단이 필요하다** — 사용자 확인 대상.

`GET /main` 은 서버에 아직 없다(양쪽 경로 다 404). `/stories` 는 401 이라 매핑돼 있다.

#### 활동 3종도 API 계층만 만들었다

`GET /sessions/{id}/post-activity` (BE 완료된 유일한 것) ·
`POST .../submit` · `POST .../retell` 을 `features/story/api/activityApi.ts` 에 붙였다.
기존 활동 화면과 대응이 정확하다 — **리텔링 응답의 `utterance_count` ·
`new_vocabulary_count` 가 완료 화면의 "N번 말했어요 · 단어 M개" 그 값이다.**

**화면에 못 붙인 이유는 `session_id` 가 없기 때문이다.** 세션은
`POST /progress/{story_id}/start` 가 만들어주는데 그쪽이 아직 BE 미구현이다.
학습(`/progress/*`) 이 붙는 순간 활동도 같이 연결된다.

명세에서 조심할 점 둘을 코드 주석에 박아뒀다.

- **카드는 매 요청마다 무작위로 섞여 온다.** 받은 순서를 정답으로 착각하면 안 된다.
  그래서 `usePostActivity` 는 `staleTime: Infinity` 다 — 아이가 배열하는 중에
  다시 받아오면 눈앞에서 카드가 뒤섞인다. 오답일 때도 무효화하지 않는다
- **`vocabulary` 는 정답일 때만 온다.** 오답이면 null 이다

#### 보호자 확인이 진짜 문이 됐다

`ParentGateModal` 이 `POST /auth/verify-password` 를 부른다. 그전에는 아무 값이나
넣어도 통과했다 — 리포트를 아이가 그냥 열 수 있었다는 뜻이다.

**비밀번호를 앱에서 비교하지 않는다.** 앱이 아는 값으로 막으면 문이 아니게 된다.
서버가 토큰의 email 클레임으로 계정을 찾아 대조한다.

**401 을 "비밀번호가 올바르지 않아요"로 안내한다.** 서버는 비밀번호 불일치와
토큰 문제를 둘 다 401 로 주는데, 이 모달은 로그인 상태에서만 열리므로 사실상
전자다. 여기서 "로그인이 필요합니다"가 뜨면 보호자는 뭘 해야 할지 알 수 없다.

#### 회원탈퇴를 붙였다

`DELETE /auth/me`. **서버가 Supabase 계정까지 지운다**(실패 시 400). 앱에서
`supabase.auth.admin` 을 쓸 일이 없다 — service role 키는 클라이언트에 없다.

**순서가 중요하다.** 서버 탈퇴가 끝난 **뒤에** `signOut()` 한다. 뒤집으면 토큰이
먼저 사라져 탈퇴 요청이 401 로 막힌다. 그다음은 자동이다 —
`SIGNED_OUT` → 캐시 비우기 → `AuthGate` 가 로그인 화면으로.

`ConfirmModal` 에 `loading` 을 넣었다. **처리 중에는 취소도 막는다** — 모달만
닫히고 요청은 계속 가는 상태가 되돌릴 수 없는 동작에서 제일 위험하다.

#### 세션 시작·조회를 붙였다 — 대화 화면 구조가 여기서 나온다

`POST /progress/{story_id}/start?child_id=` 와 `GET /progress/active?child_id=`
(`features/story/api/progressApi.ts`).

**응답의 `step` 객체가 대화 화면과 그대로 대응한다.**

```
kind: narration | dialogue | mission     → 마이크 상태·패널 구성이 여기서 갈린다
scene_description                        → 왼쪽 줄거리 패널 본문
image_url                                → 오른쪽 열 배경 (지금은 hanok-yard.jpg 한 장)
character_name/opening/closing           → 등장인물 말풍선
max_turns / turn                         → 대화를 몇 번 주고받는지
mission                                  → 미션 패널
```

**`{child_name}` 은 서버가 아이 이름으로 치환해 보낸다** — 앱에서 만들지 않는다.

**409 를 실패로 다루면 안 된다.** 아이당 `in_progress` 세션은 하나뿐이라, 다른
이야기가 진행 중이면 409 가 온다. 화면에서는 "읽던 이야기를 마저 볼까요?"로
안내해야 한다. `ApiError.kind === 'conflict'` 로 갈린다.

나머지 4개(`단계 진입` · `내레이션 완료` · `아이 발화` · 상태 조회)도 붙였다.
명세에서 조심할 점을 코드 주석에 박아뒀다.

- **`speak` 만 `multipart/form-data` 다.** `apiClient` 기본 헤더가
  `application/json` 이라 여기서 덮어쓴다. 안 그러면 서버가 본문을 못 읽는다.
  RN 의 `FormData` 는 `{ uri, name, type }` 객체를 파일로 취급한다(웹의 `File` 이 없다)
- **단계 진입 응답의 `mission` 은 항상 null 이고 `character_closing` 도 안 온다.**
  둘 다 대화가 진행돼야 나오는 값이라 `speak` 응답으로 온다.
  `mission` 은 문자열이 아니라 `{ condition, examples }` 객체다
- **`GET /progress/{story_id}` 는 세션이 없으면 404 다** (빈 응답이 아니다).
  "아직 시작 안 함"으로 읽어야 해서 재시도를 껐다
- **`completeStep` 은 내레이션 전용이다.** 대화가 안 끝났으면 409 다

**⚠️ 명세에 서버가 직접 적어둔 것: STT·검증·대사 생성이 아직 비어 있다.**
지금 붙이면 `child_text` 가 빈 문자열로 오고 **대화 장면이 끝나지 않는다.**
대화 화면을 연결하기 전에 BE 확인이 필요하다.

#### 비밀번호 찾기도 붙였다 — 가입과 같은 OTP 흐름이다

3단계(이메일 → 인증코드 → 새 비밀번호)가 회원가입과 구조가 같아서 훅을 공유한다.
`useSendEmailCode(shouldCreateUser)` 로 갈린다.

**비밀번호 찾기는 `shouldCreateUser: false` 다.** true 로 두면 오타 친 주소로
새 계정이 조용히 생기고, 보호자는 "재설정했는데 로그인이 안 되는" 상태에 빠진다.

마지막 단계는 `useResetPassword` 로 따로 뒀다 — `sync-profile` 을 부르지 않는다.
기존 계정이라 서버 프로필이 이미 있어서 부르면 매번 409 다.

**⚠️ 같은 `otp_disabled` 코드가 흐름에 따라 뜻이 다르다.** 기기에서 눌러보고 알았다.

| 흐름                             | `otp_disabled` 의 뜻          |
| -------------------------------- | ----------------------------- |
| 회원가입 (`shouldCreateUser: true`)  | 콘솔에서 OTP 가입이 꺼져 있다 |
| 비밀번호 찾기 (`false`)          | **그 이메일로 가입된 계정이 없다** |

공용 문구를 그대로 쓰면 "지금은 인증코드로 가입할 수 없습니다"가 떠서 보호자가
이메일을 다시 볼 생각을 못 한다. `FindPasswordScreen` 에서만 덮어썼다.

**이 화면은 실기기에서 실제로 확인했다** — 없는 이메일로 발송을 누르면
"가입되지 않은 이메일이에요"가 뜨고 2단계로 넘어가지 않는다. (메일이 실제로
오는지는 여전히 확인 불가 — SMTP 문제 그대로다.)

#### 아이 프로필 사진 올리기를 붙였다 — 네이티브 모듈이 하나 늘었다

디자인(92:808)의 "사진 올리기" 칸이 그동안 눌러도 아무 일이 없었다. 이제 시스템
사진 선택기가 뜨고, 고른 사진이 그 자리에 원형으로 들어간다.

**⚠️ `expo-image-picker` 를 새로 넣었다. APK 재빌드가 필요하다.**
JS 만 바뀐 게 아니므로 기존 dev client 로는 안 돌아간다.

```bash
npx expo run:android --no-bundler
```

업로드는 3단계다. **파일이 우리 서버를 거치지 않는다.**

```
① POST /users/profile-image/presigned-url → { upload_url, object_key }
② PUT  <upload_url>  (파일 본문)          → 저장소(S3 등)로 직접
③ object_key 를 자녀의 profile_image_url 로 저장
```

**②만 `fetch` 를 직접 쓴다.** `apiClient` 를 태우면 baseURL 이 붙고, 무엇보다
인터셉터가 `Authorization` 을 실어서 S3 가 "서명과 Authorization 을 같이 보낼 수
없다"며 거부한다. AGENTS 의 axios 금지 규칙은 우리 API 얘기라 여기엔 걸리지 않는다.

**사진 업로드가 실패하면 아이를 만들지 않는다.** 아바타로 대신 만들어 두면
사용자는 사진이 올라간 줄 알고 넘어간다.

아바타와 사진은 **둘 중 하나만** 쓴다 — 사진을 고르면 아바타 선택이 해제되고,
아바타를 다시 고르면 사진이 지워진다.

**백엔드에 확인할 것 둘.** 명세에 없다.
- `target` 에 어떤 값이 들어가나 (지금은 `'child'` 를 보낸다)
- `object_key` 를 그대로 `profile_image_url` 에 넣어도 되나, 아니면 공개 URL 을
  따로 만들어야 하나

#### 소셜 로그인은 일부러 안 했다

HANDOVER 우선순위상 다음이었지만 남겨뒀다. 확인 못 한 게 셋이다.

- 콘솔에 어떤 provider 가 Enabled 인지 모른다
- `goodquestion://oauth` 가 Redirect URL 에 등록됐는지 모른다
- PKCE 인지 implicit 인지에 따라 복귀 처리가 달라진다
  (`exchangeCodeForSession` vs `setSession`)

셋 다 모르는 채로 쓰면 **돌려볼 수 없는 인증 코드**가 생긴다. 그래서 코드를
남기는 대신 확인할 것만 적어뒀다. `expo-web-browser` 를 넣으면 네이티브
재빌드가 필요하다는 점도 감안할 것 — `Linking.openURL` 로 우회할 수는 있다.

#### 로그아웃하면 쿼리 캐시를 비운다

`startAuthTokenSync()` 가 `SIGNED_OUT` 에서 `queryClient.clear()` 를 부른다.
안 지우면 다른 계정으로 다시 로그인했을 때 **이전 사용자의 아이 목록·단어장이
잠깐 그대로 보인다** (TanStack Query 는 캐시를 먼저 보여준다).

### 퍼블리싱 화면은 빠짐없다 (2026-08-15 전수 확인)

스냅샷에서 섹션별 화면 프레임을 전부 뽑아 구현과 대조했다.
**퍼블리싱 섹션(182:303)의 1024×768 프레임 36개 + 보호자 리포트 4개 = 전부 구현돼 있다.**

화면이 아닌데 목록에 섞여 나오는 노드 둘은 컴포넌트 스펙이고 이미 구현돼 있다.

| 노드      | 무엇                         | 구현             |
| --------- | ---------------------------- | ---------------- |
| `105:883` | 하단 탭 바 4상태 스펙        | `BottomTabBar`   |
| `133:339` | 세그먼트 탭(전체/이야기별)   | `SegmentedTabs`  |

`182:304`(퍼블리싱 금지)에는 여전히 옛 주간/월간 요약(`118:210`) 하나뿐이다.

**다시 확인하려면** — 스냅샷에서 섹션 바로 아래 프레임 중 폭 500 이상만 뽑으면
화면 후보가 나온다. 리포트는 1007 폭이라 1024 로만 거르면 빠진다.

### 색상은 Figma Variable 과 전부 일치한다 (2026-08-15 확인)

`get_variable_defs` 로 퍼블리싱 섹션·보호자 리포트·이야기 세 곳의 Variable 을
뽑아 `theme/colors.ts` 의 `palette` 와 1:1 대조했다. **17개 값 전부 일치.**
하드코딩된 색·폰트도 `grep` 으로 0건이다.

**주석이 틀렸던 걸 고쳤다.** `Netutral/200` `600` `700` 을 "Variable 미등록"이라고
적어놨는데 실제로는 등록돼 있다. 지금 미등록인 건 `neutral100/900`, `danger500`,
`success500`, `warning500`, `peach*` · `blue50` · `brown900` 뿐이다.

### Figma 확인 결과 — 구현할 변경 없음 (2026-08-15)

`get_metadata(0:1)` 스냅샷을 8/14 것과 비교했다: **추가 60 · 삭제 0 · 크기변경 1.**

추가된 60개는 전부 `이야기` 섹션(229:352)에 **떠 있는 스케치 3덩어리**다
(`417:562` `417:616` `417:648`). 화면 프레임 안이 아니라 각 이야기 화면 **오른쪽
옆 캔버스에** 놓인 108px 짜리 판이고, 단어(늑대·문·무섭다·열다)에 체크박스가
붙은 목록이다. 구현 대상이 아니라고 봤다:

- 1024×768 프레임 어디에도 속하지 않는다
- 프로젝트 아이콘이 아니라 아이콘 라이브러리 인스턴스다(`carbon:checkbox`, `mdi:checkbox-outline`)
- 단어가 자리표시다("성교" 같은 오타 포함)

**디자이너에게 이게 무슨 UI 인지 물어볼 것.** 단어 선택 팝업으로 보이는데
어느 화면에 붙는지가 없다.

크기변경 1건은 `212:261`(대화 화면의 "다시 듣기" 줄) 120 → 123 인데, 코드가 이
높이를 잡고 있지 않아(오토레이아웃) 반영할 게 없다.

### 아직 확인 안 된 것

- **로그인 성공 경로** — 테스트 계정이 없어 `sync-profile` → `/child/select` 이동과
  가드의 2번 규칙(세션 있을 때 로그인 화면 건너뛰기)을 아직 못 돌렸다.
  실패 경로(잘못된 자격증명·잘못된 이메일·네트워크 단절)는 전부 확인했다
- **연동한 API 전부** — 자녀·단어장 모두 실제 응답을 한 번도 못 받아봤다.
  계정이 생기면 **제일 먼저 이것부터** 확인한다: 아이 목록 조회 → 아이 추가 →
  단어장 조회 → 하트 토글

### 이미 깔아둔 기반 (2026-08-15)

연동의 공통 부분은 끝나 있다. 다음 세션은 로그인 화면부터 붙이면 된다.

| 무엇                                         | 어디                                                |
| -------------------------------------------- | --------------------------------------------------- |
| Supabase 클라이언트 (AsyncStorage 세션 유지) | `shared/api/supabase.ts`                            |
| 세션 토큰 → `apiClient` 자동 연결            | `startAuthTokenSync()`, `app/_layout.tsx` 에서 호출 |
| 401/403/404/409 구분                         | `shared/api/errors.ts` 의 `ApiErrorKind`            |
| env 검증 (끝 슬래시 제거, URL 경로 경고)     | `shared/config/env.ts`                              |

`@supabase/supabase-js` 와 `react-native-url-polyfill` 을 넣었다. **둘 다 순수 JS라
네이티브 재빌드가 필요 없다** — 기존 dev client APK 를 그대로 쓸 수 있다.
polyfill 은 `_layout.tsx` 최상단에서 import 하며, supabase 클라이언트보다 먼저
로드돼야 해서 위치를 옮기면 안 된다.

**화면에서 토큰을 직접 다룰 일은 없다.** `onAuthStateChange` 가 로그인·로그아웃·
토큰 갱신을 전부 따라가며 `setAuthToken()` 을 부른다.

### 서버가 아직 대부분 미구현이다

명세의 `BE 진행상황` 이 채워진 건 **"퀴즈 카드 받아오기"(`GET /api/sessions/{session_id}/post-activity`)
하나뿐**이고 나머지는 비어 있다. **"로그인부터 순서대로"는 지금 막힌다.**

그래서 이 순서를 제안한다.

1. **공통 기반** — `/api` prefix 규칙 확정, Supabase 클라이언트 도입,
   토큰을 `setAuthToken()` 에 연결, `ApiError` 를 명세의 401/403/404/409 에 맞추기
2. **BE 완료된 "퀴즈 카드 받아오기"부터 실제로 붙여** 연결·인증·에러 처리가 도는지 확인
3. 나머지는 BE 가 완료되는 순서대로

### 리포트 API 는 명세에 없다

보호자 리포트 화면은 만들어져 있는데 해당 엔드포인트가 명세에 없다.
당분간 `features/report/model/types.ts` 의 목데이터로 둔다. 백엔드에 물어볼 것.

## 막혀 있는 것

**모든 화면이 아직 목데이터로 돈다.** 명세는 나왔지만(위 참고) 서버가 대부분
미구현이라 실제로 붙인 곳은 아직 없다. 코드에 `TODO` 로 표시된 자리가 연동
지점이다(로그인·아이 등록·TTS·STT·장면 배경·이미지 선택기 등).

**대화·활동은 PRD 의 핵심인데 아직 껍데기다.** 화면과 흐름은 다 있지만 아이 말을
실제로 듣지 않는다. TTS·STT·장면 생성 API 가 붙어야 진짜로 돈다.

**리포트도 목데이터 두 편이 전부다.** 실제로는 이야기를 끝낼 때마다 서버가 대화
기록을 분석해 한 편씩 만들어야 한다. 지금은 이야기 완료와 리포트가 이어져 있지
않다 — 완료 화면에서 리포트로 가는 길도 아직 없다(디자인에도 없다).

**활성 아이가 여전히 라우트별 `useState` 다.** 홈에서 고른 아이가 리포트에
이어지지 않는다. 전역 스토어(zustand)로 올려야 한다 — 다음 후보다.

**회원탈퇴는 아직 TODO 다.** `DELETE /auth/me` 가 명세에 있고 서버에도 매핑돼
있어 보이지만(401 응답), 되돌릴 수 없는 동작이라 테스트 계정 없이 붙이지 않았다.
`MypageScreen` 의 `WithdrawModal` 자리에 그대로 남아 있다.

## 앞으로 테스트는 실기기 태블릿에서 한다 (2026-08-15 결정)

에뮬레이터가 아니라 **USB 로 연결된 태블릿에 빌드해 올려서** 확인한다.
지금 태블릿이 연결돼 있다.

```bash
adb devices                                    # 잡히는지 먼저 확인
adb install -r android/app/build/outputs/apk/debug/app-debug.apk
adb reverse tcp:8081 tcp:8081                  # Metro
adb reverse tcp:8080 tcp:8080                  # 백엔드를 PC 에서 돌릴 때
npx expo start --dev-client --clear
```

- **JS 만 바뀌었으면 APK 를 다시 만들 필요가 없다.** `@supabase/supabase-js` 도
  순수 JS라 기존 APK 그대로 쓴다. 네이티브 모듈을 새로 넣을 때만 `npx expo run:android`.
  **2026-08-15 에 `expo-image-picker` 를 넣어 한 번 재빌드했다** — 그 이전 APK 로는
  프로필 사진 화면에서 터진다.
- `.env` 를 고쳤으면 **반드시 `--clear`** 로 재시작한다. 빌드 타임에 치환되는 값이라
  Fast Refresh 로는 안 바뀐다.
- USB 로 볼 때 `EXPO_PUBLIC_API_BASE_URL` 은 `http://localhost:8080` 이면 된다
  (`adb reverse` 를 걸었을 때). 에뮬레이터의 `10.0.2.2` 와 다르다.
- 화면 확인은 `adb exec-out screencap -p > a.png`.
- 로그인 없이 화면만 볼 때는 딥링크가 빠르다
  (`goodquestion://home`, `goodquestion://story/3/play`, `goodquestion://report`).

## 다음 세션이 할 일 (우선순위 순)

0. **팀원에게 Supabase 4가지 요청** (위 "프로젝트가 우리 계정 소유가 아니다" 참고).
   이게 안 풀리면 1·2가 전부 막힌다
1. **로그인 성공 경로를 끝까지 돌려보기** — 테스트 계정으로 로그인 →
   `sync-profile` → `/child/select` 이동까지. 실기기 태블릿에서 확인한다
   (지금 `adb devices` 가 `unauthorized` 다 — 기기에서 USB 디버깅 허용을 눌러야 한다)
2. **소셜 로그인** — 콘솔에 어떤 provider 가 켜져 있는지 확인부터.
   `supabase.auth.signInWithOAuth()` + `goodquestion://oauth` 딥링크이고,
   이 라우트는 아직 앱에 없다. `LoginScreen` 의 `handleSocialPress` 가 TODO 다
3. **붙여둔 API 를 실제 응답으로 확인** — 자녀 목록·추가, 단어장 조회, 하트 토글,
   보호자 확인. 전부 코드만 있고 한 번도 안 돌아봤다
4. **소셜 로그인** — provider·Redirect URL·PKCE 여부를 확인한 뒤에 쓴다
   (그전에 쓰면 돌려볼 수 없는 코드가 된다. 위 "소셜 로그인은 일부러 안 했다" 참고)
5. 백엔드에 확인·요청할 것 (아래 "백엔드에 물어볼 것" 참고)
6. **이야기·대화 화면 연동** — `GET /stories/{id}` 와 서버 STT 가 준비되면
   목록·상세·표지·대화를 한꺼번에. API 계층은 이미 다 있다
7. (디자이너 확인 대기 — 전부 막지 않는 것들)
   이야기 표지를 서버 이미지로 바꿀지, 캔버스에 떠 있는 단어 체크박스 스케치가 무엇인지,
   리포트 프레임 폭 1007 → 1024, 리포트 드롭다운 목록 순서,
   이야기 줄의 사람 아이콘, 마이페이지의 집 모양 아이콘, "궁금해한 어휘" 칩의 갈색 글자
8. (미결) 리포트 줄간격을 시안 렌더에 맞춰 좁힐지 — 하면 읽는 영역이 ~19dp 넓어진다

## 명세가 개정됐다 (2026-08-15 저녁) — 중요

Notion 명세가 통째로 다시 만들어졌다(데이터베이스 id 가 전부 바뀌었다).
**막고 있던 것 두 개가 풀렸고, 우리 코드가 틀린 곳이 두 군데 나왔다.**

### 풀린 것

**보고서 API 가 생겼다** — `보고서` 데이터베이스가 신설됐다.

| | |
| --- | --- |
| `GET /reports/{story_id}?child_id=` | 리포트 전문 |
| `POST /reports/{story_id}/generate?child_id=` | 생성 요청 (202) |

응답이 우리 리포트 화면과 거의 그대로 맞는다 — 대표 발화, 어휘, 표현 3항목
(`perspective_empathy` `emotion_expression` `interaction`), 논리 2항목
(`thought_reason` `outcome_solution`), 집에서 이어가기, **이전/다음 이야기 id**
(우리가 만든 페이저와 대응).

**리포트는 자동으로 안 만들어진다.** 이야기를 끝내도 생기지 않고 `generate` 를
불러야 하며, 생성이 백그라운드라 **`GET` 으로 폴링해야 한다**
(`useStoryReport` 가 `status === 'generating'` 이면 3초마다 다시 묻는다).
409 는 "이미 만드는 중"이라 실패로 다루지 않는다.

**학습 `/progress` 8개가 전부 "BE 완료"** 로 바뀌었다. 어제는 0개였다.

### 새로 생긴 기능 — 대화 중 단어 고르기

`POST /progress/{story_id}/steps/{step_index}/vocabularies` 와
`DELETE .../{scene_vocabulary_id}`. 씬마다 단어가 매달려 오고
(`step.vocabularies`), 아이가 고른 단어가 **리포트의 "궁금해한 어휘"와
`GET /vocabulary?kind=curious` 의 출처**가 된다. 응답은 그 씬의 단어 전체라
화면은 통째로 갈아끼우면 된다.

### 우리 코드가 틀렸던 곳 (고쳤다)

1. **`kind` 에서 `mission` 을 뺐다.** 명세가 "미션은 kind 가 아니다"라고
   못박았다 — 미션이 있는 장면도 `kind` 는 `dialogue` 이고 `step.mission`
   객체가 따로 붙는다. **미션 여부는 `mission !== null` 로 판단해야 한다**
2. **`step.vocabularies` 를 추가했다.** 예전 스펙에는 없던 배열이다

### ⚠️ 단어장 스펙이 두 가지로 충돌한다 — 백엔드 확인 필요

같은 `GET /vocabulary` 에 대해 명세 안에 서로 다른 스펙이 **둘 다** 있다.

| | 단어장 DB (옛 버전) | 보고서 페이지에 붙은 것 (새 버전) |
| --- | --- | --- |
| 경로 | `/api/vocabulary` | `/vocabulary` |
| 응답 | 배열 | `{ total, items }` |
| 필드 | `story_id` `story_title` `is_saved` | `kind`(used/curious) `definition` `example_sentence` |
| 페이징 | 없음 | `limit` / `offset` |

새 버전이 맞아 보인다 — 새로 생긴 단어 고르기 기능이 "`GET /vocabulary?kind=curious`
의 출처"라며 새 버전을 가리키고, 경로도 `/api` 없이 적혀 실제 서버와 맞는다.

**그런데 새 버전에는 `is_saved` 도 `story_title` 도 없다.** 우리 단어장 화면에는
하트(저장 토글)와 "이야기별" 탭이 있고, `POST/DELETE /vocabulary/{id}/save` 는
명세에 그대로 남아 있다. **앱이 정할 수 없는 충돌이라 지금 구현을 그대로 뒀다.**

## 대화·활동 화면을 서버에 붙였다 (2026-08-16)

**장면을 서버가 굴린다.** 앱이 장면 수를 세지 않는다.

```
POST /progress/{story_id}/start            → 세션 + 첫 장면
  나레이션 장면: POST .../steps/{n}/complete → 다음 장면
  대화 장면:     POST .../steps/{n}/speak    → scene_ended 가 true 가 될 때까지
POST /progress/{story_id}/steps/{n+1}      → 다음 장면 진입
```

**`expo-audio` 를 새로 넣었다 (APK 재빌드 필요).** `speak` 가 m4a 를 요구하는데
`RecordingPresets.HIGH_QUALITY` 가 마침 `.m4a`(mpeg4/aac)라 그대로 쓴다.
**마이크 권한은 화면에 들어올 때 미리 받는다** — 아이가 마이크를 처음 누른
순간에 팝업이 뜨면 무슨 창인지 모르고 닫아버린다.

**씬 단어 고르기(`SceneWordPicker`)를 새로 만들었다.** 디자인에 확정 시안이
없어서(캔버스에 떠 있던 체크박스 스케치가 이것으로 보인다) 기존 토큰으로 만들되,
**단어장의 "궁금해한 어휘" 칩과 같은 색**을 썼다 — 나중에 단어장에서 다시 만나는
말들이라 처음 고르는 자리에서도 같은 색이어야 이어진다.

**순서 맞추기의 정오답은 서버가 판정한다.** 앱은 정답 순서를 모른다.
카드는 매 요청마다 섞여 오므로 `staleTime: Infinity` 로 두고, 오답일 때는
무효화하지 않는다 — 아이가 방금 놓은 배열이 눈앞에서 뒤섞이면 안 된다.

**다시 말하기는 녹음까지만 된다.** `POST .../retell` 은 완성된 **텍스트**를 받는데
아이 음성을 글로 바꿔줄 엔드포인트가 명세에 없다(대화의 `speak` 는 장면에 묶여
있어 여기서 못 쓴다). 녹음은 해두고 `transcript` 는 빈 채로 보낸다 —
**백엔드 확인 대상.**

## 리포트 화면을 서버에 붙였다 (2026-08-16)

**리포트는 자동으로 안 만들어진다.** 이야기를 끝내도 생기지 않아서
"없음 → 만드는 중 → 완료/실패" 상태가 필요한데 **디자인에 없다.**
새 모양을 만들지 않고 기존 `EmptyState` + `Button` 으로 `ReportPlaceholder` 를
짰다. 생성은 202 로 받고 `status` 가 `generating` 이면 3초마다 다시 묻는다.

**서버 응답을 화면 타입으로 옮기는 `model/fromApi.ts` 를 뒀다.** 컴포넌트들이
디자인에 맞춰 이미 짜여 있어서 그쪽을 뜯는 대신 한 번 변환한다. 필드명 대응표가
그 파일에 있다.

**이야기 선택 드롭다운(320:197)을 뺐다.** 서버가 리포트를 한 편씩만 주고 목록을
주지 않는다(이전/다음 이야기 id 만 온다). 이동은 아래 페이저 버튼이 담당한다.
**리포트 목록 API 가 생기면 `StoryPicker` 를 되살리면 된다** — 컴포넌트는 남겨뒀다.

디자인과 어긋난 자리 둘도 넓혔다. `strength`/`tip` 은 **둘 중 하나만** 올 수
있어서 고정 두 줄이던 `notes` 를 배열로, `quotes` 는 빌 수 있어서 빈 인용 상자를
그리지 않게 했다. `phraseNote`(말버릇 해설)는 **서버에 대응 필드가 없어** 비워둔다.

## 백엔드에 물어볼 것 — 진짜 막히는 것만

명세가 비어 있어도 **앱 쪽에서 감당할 수 있는 건 전부 감당하도록 만들어 뒀다**
(아래 "물어보지 않고 정한 것" 참고). 남은 건 앱에서 만들어낼 수 없는 셋뿐이다.

1. **서버를 다시 켜달라.** 모든 경로가 530(`error code: 1033`, Cloudflare Tunnel
   끊김). 붙여둔 API 를 하나도 못 돌려보고 있다. **이게 지금 제일 급하다**
2. **`GET /stories/{id}` 상세가 없다.** 그래서 상세 화면에서 줄거리·역할 안내·
   등장 인물 블록을 뺐다. 목록이 주는 값만 보여주는 중이다
3. **다시 말하기(retell)에 STT 가 없다.** `retell` 은 텍스트를 받는데 아이 음성을
   글로 바꿔줄 엔드포인트가 없다. 지금은 빈 텍스트를 보낸다
4. **단어장 `GET /vocabulary` 스펙이 두 개다.** 위 "충돌" 표 참고.
   어느 쪽이 맞는지, 그리고 새 버전이 맞다면 우리 화면의 하트와 "이야기별" 탭을
   어떻게 할지(=`is_saved`·`story_title` 을 살릴지)
5. **완료 화면의 "발화 횟수 / 새로 배운 단어"** — 원래 `retell` 응답이 주는
   값인데 그 응답은 앞 화면에서 한 번 쓰고 사라진다. 다시 받아올 데가 없어
   세션 정보(장면 수·카드 수)로 대신하고 있다. 서버가 다시 줄 수 있나?

~~보호자 리포트 API~~ → 2026-08-15 저녁 명세에 생겼다. 붙였다.
~~STT·대사 생성~~ → `/progress` 가 전부 "BE 완료"로 바뀌었다. 실제 동작은 서버가
올라와야 확인 가능하다.

**막히지는 않지만 알려주면 좋은 것** (답이 없어도 진행에 지장 없음)

- 명세 상세 페이지의 EndPoint 가 `/api/...` 로 적혀 있는데 서버는 `/api` 없는
  경로만 받는다. 코드는 서버에 맞춰 뒀고, 명세 표기만 고치면 된다
- `GET /main` 이 아직 404 다 (다른 엔드포인트는 401 이라 매핑돼 있다).
  홈은 `/progress/active` + `/stories` 조합으로도 만들 수 있어 대안이 있다

## 물어보지 않고 정한 것

명세에 없거나 애매해서 물어볼 수도 있었지만, **앱에서 결정하고 안전하게 감싸는
쪽을 택했다.** 나중에 서버가 다르게 정하면 표시한 자리만 고치면 된다.

| 애매했던 것                         | 정한 것                                                     | 서버가 바뀌면        |
| ----------------------------------- | ----------------------------------------------------------- | -------------------- |
| 자녀에 아바타 필드가 없다           | `profile_image_url` 에 `avatar:bear` 표식을 넣는다           | `avatarRef.ts` 만    |
| `object_key` 를 그대로 써도 되나    | **`http(s)` 로 시작할 때만 사진으로 인정**, 아니면 기본 아바타 | 손댈 것 없음         |
| 업로드 `target` 값 목록이 없다      | `'child'` 를 보낸다. 틀리면 400 이 오고 그때 고치면 된다      | `profileImageApi.ts` |
| 생년을 보낼 자리가 없는데 age 가 온다 | 나이가 0 이면 "0세" 대신 빈 칸                              | 손댈 것 없음         |
| `sync-profile` 의 `name` 출처       | 소셜 메타데이터 → 이메일 @ 앞부분 순                        | `profileName.ts`     |

**보호자 이름은 가입 단계에서 받지 않기로 했다** (2026-08-15 사용자 결정).
시안에 이름칸이 없어서 이메일 @ 앞부분이 마이페이지에 그대로 보이는데, **시안을
건드리지 않고 나중에 '계정 설정' 화면에서 고치게 한다.** 그 화면은 지금 디자인
준비 중이고, 나오면 `PATCH /users/me`(`useUpdateParentProfile`, 이미 구현돼 있다)를
붙이면 된다.

**`object_key` 건이 특히 중요하다.** 키가 그대로 저장되더라도 화면이 깨지지 않고
기본 아바타로 떨어진다. 서버가 공개 URL 로 바꿔주면 코드를 손대지 않아도 사진이 뜬다.
